# Cricket Sim · Project Roadmap

Mobile **roguelite cricket-career** mashing **Reigns × Balatro × management**, targeting South Africa & Australia. ~2:45 per match. Personal/learning project — "success" = I play it and enjoy it.

---

## Current status

**Phase:** Build · **Theme 7 (Tech foundation → match sim) underway on `main`.** Player Creation V1 **and** the Hall of Fame real screen are both **done and merged to `main`** (Player Creation Phases 0–11; HoF via PRs #1/#2). Theme 7's sim rungs merged so far: **`resolve_ball()`** atom (PR #3), **single-innings batting sim** (PR #5), **full-match sim** (PR #6), **per-phase Intent / 4a** (PR #7), **per-phase bowling rotation / 4b** (PR #8), **Season-wrapper rung 7b-1** (PR #9), a **harness-driven balance tune** (Tour spread 3.0→1.5, PR #11), and the **Season league phase 7b-2a** (PR #12). **169 tests green on `main`.**
**Last closed:** **Theme 7b rung 7b-2a — the Season league phase** (2026-06-07, PR #12). Loops 7b-1's match into an **8-team single round-robin** (28 games, each team plays 7) and produces a real standings table + the Player's finishing position. Three new pieces: **`StandingsRow`** (`scripts/data/standings_row.gd` — a team's league record: points + runs/balls for & against, with `nrr()` Net Run Rate, actual-balls V1 simplification of the all-out quota rule), **`LeagueResolver`** (`scripts/domain/league_resolver.gd` — `round_robin(n)` fixtures + `simulate_league(...)`), and **`LeagueResult`** (`scripts/data/league_result.gd` — ranked `standings`, `player_position` 1–8, `made_playoffs` = top 4, `player_matches`). `simulate_league` derives each Team's strength **once per Season** (held all Season, ADR 0009 — the "noise hoist"), simulates **all 28 games** (Player games statted via `simulate_match`; non-Player games via the already-supported `simulate_match(null, …)` derived path — one code path, a genuine table), accumulates points (win 2/tie 1/loss 0) + NRR, and ranks **points → NRR → index**. **Eyeball deliverable:** `tools/league_preview.gd` runs the real league phase sweeping the Player team's ★ against a **random** 7-opponent field; `docs/mockups/league-finish-v1.html` charts **playoff-qualification rate + average finish vs ★** — both climb with ★, qualification crosses 50% at ~★3.5 (the mid-curve step is the natural sigmoid of a hard top-4 cut on 8 teams). **Scope call (documented in spec):** split 7b-2 into **7b-2a (league phase, this)** + **7b-2b (playoffs + beat/win outcome + ★ Markov mutation)**, mirroring the 4a/4b + 7b-1/7b-2 splits. Built **inline test-first** (4 tasks). 6 new tests (169, was 163): round_robin coverage; NRR sign/zero; league determinism + structural integrity + table-sorted + directional (5★ qualifies more + finishes higher than 0.5★). Spec `docs/superpowers/specs/2026-06-07-season-league-7b2a-design.md`, plan `docs/superpowers/plans/2026-06-07-season-league-7b2a.md`. **Known:** pre-existing HoF scene-test orphans unchanged (added only pure domain logic + diagnostics).
**Prior slice:** **Theme 7b rung 7b-1 — Team · stars→numbers · toss · Team-bundled match interface** (2026-06-07, PR #9). Also a follow-up **harness-driven balance tune** (PR #11): Tour `spread` 3.0 → 1.5 (and made `float`) to flatten the underdog drop-off after Nico flagged it too steep — a 2-tier underdog now wins ~17% (was ~3%). The sim finally speaks the game's real vocabulary — ★-rated Teams, a Tour that sets strength, a simulated toss — layered on top of the **byte-identical** `simulate_match()` core (same additive discipline as 4a/4b). Three new pieces: **`TourDistribution`** (`scripts/data/tour_distribution.gd` — a Tour's strength band `mean ± spread`; `percentile(frac)` maps a `stars/5` fraction → a strength number on the ~1–8 attribute scale; strawman mean 5 / spread 3 / noise 1, harness-tuned later), **`Team`** (`scripts/data/team.gd` — a ★-rated side 0.5–5.0; `batting_strength()`/`bowling_strength()` derive per-Match from `tour.percentile(stars/5) + noise`, two independent draws floored at 1, per ADR 0009), and the **toss** (`MatchResolver._resolve_toss()` — seeded 50/50 coin-flip replacing the caller-supplied `player_bats_first` bool). New entry point **`MatchResolver.simulate_match_teams(player, playerTeam, oppTeam, tour, …)`** derives the six strength ints + flips the toss (fixed RNG draw order: toss → 4 strength draws → core sim) and delegates to the existing core (not modified). **Eyeball deliverable** (Nico's "learn by seeing" ask): `tools/season_preview.gd` runs the **real** sim across star matchups, `docs/mockups/star-winrate-v1.html` charts **win-rate vs star gap** — a clean S-curve crossing ~54% at gap 0 (the slight edge is the statted Player on the Player's side), 0% at −4.5, 100% at +4.5. First concrete seed of the 7c balance harness. **Scope call (documented in spec):** split 7b into **7b-1 (this — play one Match between two Teams)** + **7b-2 (the Season league loop)**, mirroring the 4a/4b split. Built **inline test-first** (4 tasks: TourDistribution, Team, toss+wrapper, runner+chart). 9 new tests (163, was 154): percentile endpoints/monotonic/clamp; star→strength direction + noise bound/floor + zero-noise==percentile; match determinism + directional (5★ beats 0.5★ >80%) + even-teams balance. Also added `.claude/launch.json` (static preview server for `docs/mockups`). Spec `docs/superpowers/specs/2026-06-07-season-wrapper-7b1-design.md`, plan `docs/superpowers/plans/2026-06-07-season-wrapper-7b1.md`. **Known:** pre-existing HoF scene-test orphans unchanged (7b-1 added only pure domain logic + diagnostics).
**Prior slice:** **Theme 7a rung 4b — per-phase bowling rotation** (2026-06-07, PR #8). The bowling side gets its first decision lever — the mirror of 4a Intent, and the sim realization of `setNextBowler` (ADR 0006). Two new value objects: **`BowlingPlan`** (`scripts/data/bowling_plan.gd` — `enum Kind {PACE, SPIN}`, one type per phase 1–6/7–15/16–20, `for_over()`, `textbook()`/`pace_only()`/`spin_only()` factories; bare `new()` == textbook pace/spin/pace) and **`BowlingAttack`** (`scripts/data/bowling_attack.gd` — derives pace/spin `(attack, control)` profiles from a base pair ± a tilt constant, default 2, floored at 1; `profile(kind) → Vector2i`). Threaded as optional trailing params through `simulate_innings()` (per-over profile via `balls / 6 + 1`; null → constant scalar pair) and `simulate_match()` (opt-in `player_bowling_plan`: null → no rotation either side, byte-identical to 4a; set → Player's team bowling uses the plan, routed to the innings where the opposition bats, opposition bowls a fixed `textbook()` AI default). **The decision is a real EV trade (ADR 0003):** pace = attack-tilted (more wickets **and** more runs); spin = control-tilted (contains, threatens less) — pace for wickets, spin for containment, neither dominates. **Scope calls (documented in spec):** narrowed 4b to the rotation *mechanism* — **bowler husbanding (finite pool, 4-over cap, fatigue/decay) deferred to a possible 4c**; **Player stays bat-only** (team bowling from team strength; Player-as-bowler deferred). Built **inline test-first** (4 tiny pure-domain tasks, zero isolation surface). 10 new tests (154, was 144): BowlingAttack tilt + floor; BowlingPlan phase mapping + factories; innings null==scalar regression + determinism + directional run-rate (pace more wickets at higher run rate than spin over 40 innings); match null==4a regression + routing (first-innings isolation trick) + determinism. Spec `docs/superpowers/specs/2026-06-07-bowling-change-design.md`, plan `docs/superpowers/plans/2026-06-07-bowling-change.md`. **Known:** pre-existing HoF scene-test orphans unchanged (4b added only pure domain logic).
**Prior slice:** **Theme 7a rung 4a — per-phase batting Intent** (2026-06-07, PR #7). The sim stops hardcoding `Intent.BALANCED`: a small `IntentPlan` value object (`scripts/data/intent_plan.gd`) holds one Intent band per phase (Powerplay 1–6 / Middle 7–15 / Death 16–20), with `for_over()` lookup and `balanced()` + `textbook()` factories (phase boundaries as constants — `POWERPLAY_OVERS=6` per T20 law, `DEATH_START_OVER=16` tunable). Threaded as an optional trailing param through `simulate_innings()` (null → all-BALANCED, byte-identical to rung 3) and `simulate_match()` (routed to the **Player's batting innings only**; opposition stays Balanced). This is the sim's **first real decision input** — the lever the ADR 0003 naive-vs-optimal skill-gap metric measures; the Theme-6 swipe-card later writes into this same seam. **Rung 4 was split into 4a (Intent, done) + 4b (Bowling Change, next).** Built **inline test-first** (not subagent-driven — 3 tiny pure-domain tasks with zero test-isolation surface, the bug class subagent review exists to catch). 11 new tests (144, was 133): IntentPlan phase mapping + factories; innings null==balanced regression, determinism-with-plan, directional coupling (aggressive both outscores **and** out-dies defensive over 40 innings); match routing both directions (proven via the first-innings-uses-initial-RNG isolation trick) + determinism. Spec `docs/superpowers/specs/2026-06-07-intent-plan-design.md`, plan `docs/superpowers/plans/2026-06-07-intent-plan.md`. **Known:** the pre-existing HoF scene-test orphans are unchanged (rung 4a added only pure domain logic).
**Prior slice:** **Theme 7a rung 3 — full-match sim** (2026-06-07, PR #6). `MatchResolver.simulate_match()` calls `simulate_innings()` twice (Player's team + opposition), runs a chase, and decides a cricket-standard result — the **thinnest-wrapper** design (constant bowling, raw-int inputs, caller-supplied toss bool). **Settled the bowler-rotation open question: deferred** — constant bowling both sides this rung; real per-over rotation lands with the Bowling Change Key Moment (next rung). Two backward-compatible extensions to `simulate_innings()`: **optional statted Player** (`null` → all 11 derived, for the opposition innings) + **optional chase `target`** (third stop condition: ends the instant `total >= target`). `_decide_result()` extracted as a pure function so all three outcomes (incl. the hard-to-seed tie) are unit-tested on constructed innings. New `MatchResult` holder (both innings, `Outcome` enum, margin fields, `player_won()`/`is_tie()`/`margin_text()`). Files: `scripts/data/match_result.gd`, `scripts/domain/match_resolver.gd`, extended `scripts/domain/innings_resolver.gd`. Full brainstorm→spec→plan→build→merge cycle; **subagent-driven** (implementer → spec review → quality review per task, + holistic final review), test-first (18 new GUT tests across 4 tasks: determinism, optional-Player, backward-compat, chase-stops-early, win-by-runs/wickets/tie, perspective mapping, even-contest balance). Spec `docs/superpowers/specs/2026-06-07-full-match-design.md`, plan `docs/superpowers/plans/2026-06-07-full-match-plan.md`. **133 tests green** (was 115 pre-rung). **Known:** the 15 pre-existing HoF scene-test orphans on `main` are unchanged (rung 3 added only pure domain logic — structurally can't add scene orphans).
**Prior slice:** **Theme 7a rung 2 — single-innings batting sim** (2026-06-07, PR #5): `InningsResolver.simulate_innings()` loops the ball atom into one deterministic T20 innings — 11 batters, **build-driven batting position** (settles participation), weakening tail, strike rotation, 10-wkt/120-ball termination. `InningsResult` + `InningsTuning` strawman. Sandbox `docs/mockups/innings-sim-v1.html`. **Prior:** rung 1 — `resolve_ball()` (PR #3), two-stage logistic, `BallTuning`.
**Next theme:** continue building the match sim **upward**: ✅ ball → ✅ innings → ✅ full match → ✅ 4a Intent → ✅ 4b bowling rotation → ✅ **7b-1 (Team + stars→numbers + toss + match interface)** → ✅ **7b-2a (league phase — schedule + table + Player finish)** → **next = 7b-2b (playoffs + beat/win outcome + ★ Markov mutation)**, then **balance harness (7c)**. 7b-2b builds on `LeagueResolver`: take the top-4 from the league table → two semi-finals (1v4, 2v3) → **The Final** (winners, 1st/2nd) + **3rd-place playoff** (losers, 3rd/4th); decide the **beat-vs-win** outcome (top 3 = beat the Season → grid progression; win The Final of a Premium tour = win the Level); and add the ★ **Markov mutation** at Season rollover (ADR 0009's ±0.5 ~30% / ±1.0 ~5% table) as a `Team.mutate_stars(rng)` primitive ready for the multi-Season Career loop. (Optional 4c bowler husbanding still deferred.)

---

## Next session

**Theme 7 (match sim) is underway on `main`.** Player Creation V1 and the Hall of Fame real screen are both done and merged. Theme 7's sim rungs so far — **`resolve_ball()`** atom (PR #3), **single-innings batting sim** (PR #5), **full-match sim** (PR #6), **per-phase Intent / 4a** (PR #7), **per-phase bowling rotation / 4b** (PR #8), **Season-wrapper 7b-1** (PR #9), the **spread balance tune** (PR #11), and the **Season league phase 7b-2a** (PR #12) — are built test-first and merged. **169 tests green on `main`.** Bottom-up progress: ✅ single ball → ✅ innings → ✅ full match → ✅ Intent (4a) → ✅ bowling rotation (4b) → ✅ 7b-1 (Team/stars/toss/match) → ✅ 7b-2a (league phase) → ⏳ **7b-2b (playoffs + beat/win + ★ mutation)** → balance harness (7c).

**To continue (next Claude Code session):**

> Run the start-of-session routine (you're on `main`, all merged). **Run the next rung AFK / autonomously** (Nico's standing preference from 2026-06-07, codified in project `CLAUDE.md`): drive brainstorm→spec→plan→build→PR-merge→roadmap on your own, make+document default decisions in the spec, only stop to ask when a decision is genuinely his. **7b-2a is done — `LeagueResolver.simulate_league()` plays a full 8-team round-robin (28 games, all simulated), builds a points+NRR table, and returns the Player's finish + `made_playoffs`; `docs/mockups/league-finish-v1.html` charts qualification + finish vs ★.** The next slice is **7b-2b — playoffs + outcome + ★ mutation** — run `brainstorming` on it. 7b-2b builds on `LeagueResolver`: (1) take the **top 4** from `LeagueResult.standings` → **two semi-finals** (seed 1v4, 2v3) → **The Final** (winners → 1st/2nd) + **3rd-place playoff** (losers → 3rd/4th) — reuse `simulate_match` with the per-Season held strengths (a playoff tie → higher seed advances, document it); (2) decide the **beat-vs-win** outcome — **beat the Season** = finish top 3 (grid progression), **win the Level** = win The Final of a Premium tour (CONTEXT.md) → a `SeasonResult` holder; (3) add a **`Team.mutate_stars(rng)`** primitive (ADR 0009's ±0.5 ~30% / ±1.0 ~5% table, clamped 0.5–5.0) ready for the multi-Season Career loop (the actual Career-spanning loop + grid is a later rung). Note Nico wants to **see distributions** — add a Season-level chart (e.g. beat-rate / win-rate by Team ★, à la `tools/league_preview.gd`). Conventions (project `CLAUDE.md`): GDScript tabs; `--import` once after adding scripts; the GUT `-gtest` flag does **not** filter — judge red by parse-error, green by the count climbing past 169; **quit the Godot editor before headless runs** (check `pgrep Godot` first); commit `*.gd.uid` for `scripts/` (and `tools/`) files, not for `tests/`. The `spread` dial is now **1.5** (tuned); `simulate_match(null, …)` gives a derived-vs-derived match. **Concise replies preferred.**

**Open auto-sim questions:** participation model — **settled at rung 2** (build-driven batting position). Bowler-rotation policy — **settled at rung 4b** (per-phase pace/spin via `BowlingPlan` + tilted `BowlingAttack` profiles). Batting Intent — **settled at rung 4a** (`IntentPlan` per phase). stars→numbers pipeline + Team interface + toss — **settled at 7b-1** (`TourDistribution` + `Team` + `_resolve_toss` + `simulate_match_teams`). Still open / deferred: **Season loop + ★ Markov mutation** (→ 7b-2), **toss bat/bowl heuristic** (winner always bats first for now), **distinct attack vs control per Team** (one bowling number for now; pace/spin tilt differentiates inside the innings), **bowler husbanding** (pool/cap/decay → optional 4c), **Player-as-bowler** (Player's Attack/Control still don't feed the sim → later rung), **opponent batting + bowling intelligence** (opposition bats Balanced, bowls textbook → later rung).

**Loose thread:** 15 orphaned nodes in the GUT suite (pre-existing, from the merged Hall of Fame scene tests) — flagged as a background task (`task_13d395f2`), not introduced by Theme 7a.

---

## The themes (ordered by dependency)

1. ~~**Scope**~~ ✅ — V1 locked: SA+AUS roguelite cricket-career (ADR 0002)
2. ~~**Game shape**~~ ✅ — Career grid (Levels × Tours), Offers/Tons/Affinity (CONTEXT.md)
3. ~~**Gameplay & balance**~~ ✅ — sim math (ADR 0004), skill model (ADR 0003), Manager Boost (ADR 0005), 6-verb effect palette (ADR 0006), all 8 KMs designed. Numerical tuning deferred to the balance harness.
4. ~~**Jokers & meta loop**~~ ✅ — Architecture (Shop cadence, authoring grammar ADR 0007, carry-over, Team durability ADR 0009) + Content (all 45 Jokers authored at `docs/joker-pool-v1.md` — 24 Common / 15 Rare / 6 Legendary across 6 archetypes, magnitudes V1 strawman for the balance harness).
5. ~~**Around-the-match screens**~~ ✅ — Season hub · Pre-match · Shop · Offer · Result · Career Grid · Career Records (ADR 0010 navigation shell · ADR 0011 global rank · hi-fi at `docs/mockups/around-the-match-v1.html` · DESIGN_HANDOFF §17). **Onboarding now closed** — Player Creation spec at `docs/superpowers/specs/2026-06-01-player-creation-design.md` covers the 2-screen Identity → Build flow + hard-permadeath lifecycle. **Hall of Fame screen spec now delivered** (`docs/superpowers/specs/2026-06-02-hall-of-fame-design.md`) — the last Theme 5 sibling-spec gap; it builds later as the real screen replacing the Player Creation plan's HoF stub. Settings still deferred.
6. **Visual & audio content** — joker art · backgrounds · icons · music · SFX · commentary bank · **8 portrait sets** (4 Appearance × 2 Country) + Player Creation backgrounds (per Player Creation spec)
7. **Tech foundation** ⏳ *in progress* — Godot project ✅ · save system ✅ · build pipeline · the **match sim** (ADR 0004, built bottom-up: ✅ `resolve_ball()` atom · ✅ single-innings sim · ✅ full match · ✅ Intent (4a) · ✅ bowling rotation (4b) · ✅ Team/stars/toss/match interface (7b-1) · ✅ league phase (7b-2a) · ⏳ playoffs + outcome + ★ mutation (7b-2b)) · **balance harness** (headless sim runner — parameter sweeps over Tour distributions, KM/Joker/Boost coefficients, and the optimal-vs-naive skill-gap metric per ADR 0003/0004)
8. **Long-haul retention** — Collection / Achievements layer (77-feat trophy cabinet across 6 categories with Tons rewards) · streaks · daily prompts · any other replay-extending systems. Deferred until after the build is playable.
9. **Career pressure & retirement** (post-launch) — auto-drop / non-selection / age-based retirement triggers. Layered on top of the hard-permadeath lifecycle established in the Player Creation spec; trigger anchors on **Player form** (low runs over last K innings, lost KMs), not Team result. Deferred until V1 ships and the Career-as-life baseline loop is playtested — the cheap escape-hatch (Manual retire button) covers the *"I'm stuck"* case in the meantime.

Playtest threads through all of them — continuous, not a discrete item.

---

## Open design questions

Surfaced 2026-06-02 while pressure-testing the Player Creation plan. Both are **auto-sim design decisions (Theme 7)** — non-blocking for the Player Creation build, but must be settled before/during the sim build. Both sit downstream of ADR 0004 (only-Player-statted; the 21 other players are derived from `teamStrength × role × seed`, not individually authored).

1. **Player participation model** — how much the Player personally bats (batting position → balls faced) and bowls (overs) each Match. Candidates: *fixed participation* (build changes only how **well**, not how **much**; bowler-shaped Players still bat, and bat badly emergently) vs *attribute-driven participation* (a bowler-shaped build bats lower / bowls more automatically). Either keeps the classifier label flavour-only per spec §3.5. **Simplest-first leaning: fixed.**
2. **Bowler-rotation policy** — how an innings' ~20 bowling overs get assigned across bowler-role profiles. *Player side* surfaces as agency via the **Bowling Change** Key Moment; *opponent side* needs an AI rotation policy (currently un-spec'd). Note the scope wall: opponents are regenerated per-Match with no persistent identity or cross-Match form, so "their star bowler is out of form" is **texture-via-commentary**, not simulation — the cheap third door that avoids rebuilding the deferred management sim.

---

## Decisions log

| Date | Decision | Rationale |
|---|---|---|
| 2026-06-07 | Theme 7b rung 7b-2a — Season league phase built + merged | Split 7b-2 into **7b-2a** (league phase, this) + **7b-2b** (playoffs + beat/win + ★ mutation), same pattern as 4a/4b + 7b-1/7b-2. Built 7b-2a (PR #12): `LeagueResolver.simulate_league()` plays an 8-team single round-robin (28 games, each plays 7), every game simulated — Player games statted, non-Player games via the already-supported `simulate_match(null,…)` derived path (one code path, real table). Each Team's strength drawn **once per Season** (held all Season, ADR 0009 noise hoist). `StandingsRow` (points + runs/balls for/against + `nrr()`), ranked **points → NRR → index**, into `LeagueResult` (standings, player_position 1–8, made_playoffs = top 4, player_matches). Decisions recorded in spec: full-sim all games (D2), per-Season draw (D3), per-match toss (D4), points 2/1/0 + NRR tiebreak with actual-balls simplification (D5). Eyeball `tools/league_preview.gd` → `docs/mockups/league-finish-v1.html` (qualification + avg finish vs ★ over random fields; both climb with ★, qual crosses 50% at ~★3.5). Inline test-first (4 tasks); 6 new tests (169, was 163). Spec `docs/superpowers/specs/2026-06-07-season-league-7b2a-design.md`, plan `docs/superpowers/plans/2026-06-07-season-league-7b2a.md`. **Next = 7b-2b.** |
| 2026-06-07 | First harness-driven balance tune — `TourDistribution.spread` 3.0 → 1.5 (and made `float`) | Nico flagged the 7b-1 win-rate curve as too steep (a 2-tier underdog beating a 5★ only ~3%; he expected ~20%). Root cause: a match aggregates ~240 balls, so even a small per-ball edge compounds into a near-certain result — the curve is inherently steeper than the per-ball gap feels. The clean dial is `spread` (how many strength points a star is worth), flagged strawman for exactly this. Ran a sweep over spread ∈ {1.0…3.0} via a throwaway `tools/spread_sweep.gd`; **1.5** best fit the desired feel — 1-tier underdog ~33%, 2-tier ~17%, minnow-vs-giant ~0%, while stars still discriminate cleanly at the extremes (spread 1.0 let the ±1 noise drown the star signal — giant only won 95.5%). Changed `spread` from `int` to `float` so a star can map to fractional strength points. Regenerated `docs/mockups/star-winrate-v1.html` at 1.5. The first real balance decision made *through* the harness — exactly its purpose (ADR 0004). 163 tests still green (unit tests pass explicit tuning values; only the default changed). |
| 2026-06-07 | Theme 7b rung 7b-1 — Team · stars→numbers · toss · Team-bundled match built + merged | Split 7b into **7b-1** (play one Match between two ★-rated Teams) + **7b-2** (the Season league loop), mirroring 4a/4b. Built 7b-1 (PR #9): the sim gains the game's real vocabulary on top of the **byte-identical** `simulate_match()` core. `TourDistribution` (`mean ± spread`; `percentile(stars/5)→strength`, strawman 5/3/1) + `Team` (★ 0.5–5.0; `batting/bowling_strength = percentile(stars/5)+noise`, 2 independent draws floored at 1, ADR 0009) + seeded toss (`_resolve_toss`, replaces the `player_bats_first` bool). New entry point `MatchResolver.simulate_match_teams(player, playerTeam, oppTeam, tour, …)` derives 6 ints + flips the toss (fixed RNG order: toss→4 draws→core) and delegates to the unmodified core. **Eyeball deliverable** (Nico's "see the distribution" ask): `tools/season_preview.gd` runs the real sim across star matchups → `docs/mockups/star-winrate-v1.html` charts win-rate vs star gap — a clean S-curve (~54% at gap 0, 0%/100% at ±4.5); first seed of the 7c balance harness. Inline test-first (4 tasks); 9 new tests (163, was 154). Added `.claude/launch.json` for static mockup previews. Spec `docs/superpowers/specs/2026-06-07-season-wrapper-7b1-design.md`, plan `docs/superpowers/plans/2026-06-07-season-wrapper-7b1.md`. **Next = 7b-2 (Season league loop + ★ Markov mutation).** |
| 2026-05-18 | In-match screen design locked | See `docs/DESIGN_HANDOFF.md` Sections 3–4, 16 |
| 2026-05-18 | Avatar: Card Portrait + split-bg | Skill tier (sky) + confidence tier (ground) + 3-layer system |
| 2026-05-18 | Match length: 2:45 | India ARPU + session length fit (research-backed) |
| 2026-05-18 | Manager Boost: water-meter | Strength = fill %; tactical-depth via timing |
| 2026-05-18 | No IPL trade dress | Personal project · steer safe on IP |
| 2026-05-18 | Soft launch markets: PAK / BAN / SLK / UAE | Cricket audience · no India burn (when launch becomes relevant) |
| 2026-05-19 | Engine: Godot 4.x | Best fit for 2D card/UI with juice; AI handles ramp |
| 2026-05-19 | Launch track deferred | Build the game first; launch playbook saved for later |
| 2026-05-19 | Next-session skill: `grill-with-docs` | Better than `brainstorming` for conceptual work; opinionated + glossary discipline |
| 2026-05-21 | V1 = roguelite cricket-career | ADR 0002 — you are one rising Player climbing a Career grid |
| 2026-05-21 | Primary target: SA + AUS | ADR 0001 — creator-fit beats market-fit; India deferred |
| 2026-05-21 | Career grid: 3 Levels × 8 Tours | Club/City/Province × named tours; overlapping difficulty bands |
| 2026-05-21 | Career layer: Offers / Tons / Affinity | Light career sim; full club-management deferred |
| 2026-05-21 | `CONTEXT.md` created | Domain glossary — source of truth for terminology |
| 2026-05-22 | Skill model locked | ADR 0003 — 3 layers (Tactical / Strategic / Meta), no-dominant-answer, never surface the math |
| 2026-05-25 | Auto-sim architecture locked | ADR 0004 — headless, deterministic-seedable, ball-by-ball, 2-stage logistic, 4 Attributes (Power/Composure/Attack/Control), only-Player-statted |
| 2026-05-26 | 5 remaining Key Moments designed | Wicket Crisis, Milestone Ball, Bowling Change, Field Set, DRS Review |
| 2026-05-26 | Portrait redesign | Full-bg = skill, facial expression = Form (supersedes split-bg, DESIGN_HANDOFF §16.3) |
| 2026-05-27 | Manager Boost simplified | ADR 0005 — no menu, side-aware all-Attribute buff, drain-on-press, fill² scaling |
| 2026-05-28 | 6-verb typed-effect palette | ADR 0006 — every in-Match mechanic composes from `setIntent` / `setNextBowler` / `fieldMode` / `buffNextBalls` / `formEvent` / `tryReview` |
| 2026-05-28 | Theme 3 closed | Gameplay & balance design complete; numerical tuning is balance-harness work |
| 2026-05-28 | Currency named Tons (₸) | ADR 0008 — "Pay" working name dropped; ₸ glyph (T struck by a bail, two bails standard) locked. Assets in `docs/brand/`. |
| 2026-05-28 | Joker authoring grammar locked | ADR 0007 — passive verb buffs + conditional triggers, nothing else; synergies emerge from shared verb tags (no authored chain logic). |
| 2026-05-28 | Team strength durability via half-★ Markov rating | ADR 0009 — Teams carry durable 0.5–5★ rating in half-star increments, ~30% ±0.5 / ~5% ±1 per Season + `lastSeasonEvent` flavour string for ±1 swings; makes Offers a real Meta-layer signal. |
| 2026-05-28 | Meta-loop shape locked | Shop cadence (5 visits/Season Slay-style) · 4 actions per visit (buy/sell/upgrade/hold) · 45-Joker pool across 6 archetypes · Joker carry-over (1 owned, end-of-Season) · slot 4 earned at first Level-win · mid-Season Offer is immediate-switch. |
| 2026-05-29 | Team durability refined to half-★ + Theme 4 closed | ADR 0009 revised — stars on 0.5–5.0 in half-star increments (was integer 1–5); ±0.5 normal swings, ±1 catastrophic. Theme 4 (Jokers & meta loop) now closed; next theme is Around-the-match screens. |
| 2026-05-30 | Theme 5 architecture locked, hi-fi pass moved to claude.ai | Decisions: **Season hub** is the persistent home within a Season (`CONTEXT.md` updated); **Shop & Offer** are forced full-screen interludes triggered by cadence (no skip, no hub-overlay); **no Main Menu** (resume-first cold start, settings via corner gear); Season hub is **single dense screen** (no scroll, no tabs); Pre-match is **versus-style** (red-gradient `[A] vs [B]` header continues into Match chrome). Mockup pass spec'd in `docs/THEME-5-HANDOFF.md`; claude.ai produces hi-fi HTML at `docs/mockups/around-the-match-v1.html`. |
| 2026-05-31 | Theme 5 closed; ADRs 0010 + 0011 written; Theme 4 reopened (content); Theme 8 added | Around-the-match hi-fi accepted (`docs/mockups/around-the-match-v1.html` + `docs/design-handoff-from-claude-2026-05-31.md`). **ADR 0010** captures the navigation shell. **Career Records adopted** (global rank `#X / 2,112` computed analytically from Tour distributions — no per-rival sim) — **ADR 0011**. **Collection / Achievements deferred to new Theme 8** (Long-haul retention). **Theme 4 reopened** as architecture ✅ + content ⏳ — the 45 specific Jokers are unauthored and are now the next session. **Kit Room** / **Kit Manager** added to CONTEXT.md as canonical world-building. Australia palette fix: was a near-clone of SA green; now amber-gold primary + green accent (DESIGN_HANDOFF §16.2 updated). Onboarding + Settings screens deferred to a later pass within Theme 5's spirit. |
| 2026-06-01 | Theme 4 content closed + Theme 5 onboarding closed + Theme 9 added | All 45 Jokers authored (`docs/joker-pool-v1.md`) across 6 archetypes / 3 rarities, magnitudes V1 strawman for the harness. **Player Creation spec** (`docs/superpowers/specs/2026-06-01-player-creation-design.md`) closes Theme 5's deferred onboarding work: 2-screen Identity → Build flow · 5 decisions (Country / City / Appearance / Name / Attributes) · hard-permadeath lifecycle with Win + Manual retire end-states · live classifier label · 4 Appearance buckets per Country (no textual labels) · cricket-pun name gen (random + re-roll, no manual override). **ADR 0012** to formalise the lifecycle decision at plan time. **New Theme 9 — Career pressure & retirement** (post-launch) holds auto-drop / non-selection / age-based retirement; trigger anchors on Player form not Team result. |
| 2026-06-01 | Soft-launch markets corrected to SA + AUS | The 2026-05-18 "PAK / BAN / SLK / UAE — no India burn" decision was made under the India-primary-target era. With ADR 0001 making SA + AUS the primary target, the soft launch logically follows the primary audience: **soft launch in SA + AUS first**, expand later. Both stores allow per-country availability restriction at upload. iOS publishing is unblocked at the account level — Nico already has an Apple Developer Program membership. Google Play Developer ($25 one-time) is unconfirmed; revisit when Android shipping becomes relevant. |
| 2026-06-02 | Player Creation build plan written + reviewed; 2 sim questions logged | Ran `writing-plans` on the Player Creation spec → 12-phase Godot build plan at `docs/superpowers/plans/2026-06-01-player-creation-plan.md` (bootstrap → data/domain TDD → Creation scenes → permadeath hooks → **ADR 0012** authored in-plan). Two external review passes; key fixes folded in: archive **deep-duplicates the Player** before `clear_player()` (else an `ext_resource` orphan crashes the Hall-of-Fame path), loads use `CACHE_MODE_IGNORE`, autoloads register at the phase their script lands, Back preserves picks, Build recap added, Godot **4.3+ hard-required**. Plan is ready but **execution choice (subagent vs inline) not yet made**. Surfaced two open auto-sim questions (Player participation model · bowler-rotation policy) — logged under "Open design questions", Theme 7, non-blocking. |
| 2026-06-02 | claude.ai Player Creation + Hall of Fame design **incorporated**; V1 attribute model kept | 5 artefacts landed from the parallel claude.ai track and reconciled against CONTEXT.md + ADRs + the Player Creation spec/plan: **Hall of Fame spec** (`docs/superpowers/specs/2026-06-02-hall-of-fame-design.md`), **decisions log** (`docs/PLAYER-CREATION-DECISIONS.md`), **Godot build-notes** (`docs/PLAYER-CREATION-GODOT-BUILD-NOTES.md`), and two mockups (`docs/mockups/player-creation-v2.html` = **locked look**, `player-creation-v1.html` = variants archive). **Decision: keep the spec §3.5 V1 attribute model** (20 pts · 1–8 · 4 labels incl. WK-Batter) — the mockup's richer **32-pt / 1–10 / 9-label** classifier is logged as a **post-launch candidate**, so the reviewed build plan executes **unchanged** (Phases 1/2/6 untouched). HoF spec is **additive** to the plan: Phases 4/7 robust (`LegendEntry` stores a full `Player` snapshot → both arc-roles derivable); the real HoF screen later replaces the Phase-7 stub and gains additive `Legend` fields (`levelsWon`, `lifetimeTons`, `immortalised`, `retiredSeason`). AUS palette `#A86E00` gold-forward confirmed canonical (already in DESIGN_HANDOFF §16.2 + all mockups; the `#FFCD00` in the outgoing brief was stale). Reconciliation banners added to the 3 incorporated docs. |
| 2026-06-03 | Player Creation build started — Phases 0–2 done; 3 latent plan bugs found + patched | Executing the reviewed plan on branch `player-creation-build` (hybrid: **inline** Phases 0–2, **subagent-driven** from Phase 3, one phase per fresh chat). **Phase 0** Godot 4.6.3 project + GUT 9.6 vendored; **Phase 1** 8 domain types authored test-first; **Phase 2** classifier. **34 tests green**, 14 commits. Three latent bugs in the "reviewed" plan surfaced only at execution and were patched into it: (1) Phase 0 `--quit` boots the not-yet-existent main scene → use `--import`; GUT `v9.3.1` doesn't exist → `v9.6.0`; run `--import` after vendoring to register class_names; (2) `enum Label` collides with Godot's built-in `Label` node class → renamed `Kind` across Task 1.3 + Phase 2; (3) `*.gd.uid` files weren't being committed. Added a project **`CLAUDE.md`** capturing Godot dev conventions. |
| 2026-06-04 | Phase 5 done — first UI phase (Identity screen); eyeball step caught a test-invisible bug | **Phase 5** built the first playable screen: `AppearancePicker` subscene (4 placeholder tiles, `appearance_selected` signal, silent-select for Back-nav) + `Identity` scene (Screen 1 — country toggle, city dropdown that resets on country change, appearance gating until Country picked, portrait preview, seeded name + 🔄 re-roll, Next gated on `identity_complete()`, full draft hydration on Back). Subagent-driven (implementer → spec → code-quality review per task). **59 tests green, zero orphans**, 6 commits. **Key lesson:** all unit tests passed but the appearance tiles were *invisible* on screen — a `flat` Button tinted with `modulate` draws nothing. Caught only by running the real window (the plan's manual eyeball step), fixed by rendering tiles via `StyleBoxFlat` (with a gold selection ring). Unit tests assert "button exists / disabled toggles" and are blind to visibility — **UI phases are never done on green tests alone.** Gotcha added to project CLAUDE.md + the learn-by-seeing memory. |
| 2026-06-04 | Phase 6 done — Screen 2 (Build) | Built the attribute-allocation screen: `build.tscn` + `build.gd` — 4 sliders (each 1–8) bound to a transient `PlayerCreationDraft`, a live POINTS-REMAINING counter (white at 0, red otherwise), the live classifier label fed by Phase 2's `Classifier`, an identity recap (portrait tint + name·city·country), and a Confirm gated on `is_valid_creation_distribution()` that builds a `Player` via `from_draft` → `SaveManager.save_player` → emits `confirmed`; Back emits `back_pressed(draft)` for Identity rehydration. Slider→draft sync uses `set_value_no_signal` on push to dodge a re-entrancy bug (writing a slider value mid-update would read the *other*, not-yet-pushed sliders back into the draft and corrupt it). Subagent-driven (implementer → spec ✅ → code-quality ✅; one minor test-isolation leak — autoload `player_save_path` not restored — found in review and fixed, same discipline as Phase 4). **64 tests green** (59 + 5 new), zero orphans, 3 commits. Eyeballed in a real 390×844 window: renders + responds correctly. Nico flagged it "looks like a skeleton, not the final product" — **expected**: this plan ships flow + logic only; visual polish (locked look in `docs/mockups/player-creation-v2.html`, real portraits, palettes, backgrounds) is a separate Theme 6 pass applied after the flow is built end-to-end. |
| 2026-06-04 | Phase 7 done — main routing + stubs + lifecycle owner; flow walks end-to-end | **Phase 7** wires the whole Player Creation flow together. (7.1) `LifecycleManager` autoload — single owner of the "end Career" transition (`manual_retire`/`win_out` → `end_career` → archive + clear Player + emit `career_ended`); (7.2) 3 stub scenes (Starting Team picker, Season hub w/ Manual-retire confirm dialog + `[DEV]` win cheat, Hall of Fame listing LegendsArchive rows); (7.3) `main.tscn`/`main.gd` router keyed on `SaveManager.has_player()`. **66 tests green, zero orphans**, 4 commits, subagent-driven (spec + code-quality review per task; one isolation-leak caught in review — router test now restores `SaveManager` paths, same lesson as Phase 6). Creating `main.tscn` **cleared the benign load warning** present since Phase 1. The plan's assumed Identity/Build APIs matched the real Phase-5/6 scenes **exactly** (zero reconciliation). Eyeballed in a real window: full cold-start flow renders + routes correctly, retire + dev-win both archive to the Hall of Fame, and **the LegendsArchive persisted across a window restart** (proving Phase-4 persistence + Phase-7 lifecycle together, live). New learn-by-seeing lesson captured: during an eyeball walk, give Nico an explicit "look at / ignore" split so stub cosmetics (e.g. a too-wide retire dialog clipping its text) don't read as defects. **ADR 0012 deferred to Phase 10 per the plan** (the roadmap had mislabelled it as Phase-7 work). |
| 2026-06-04 | Phases 3–4 done — first subagent-driven phases; 2 more latent plan bugs found + patched | **Phase 3** name generator (`NameBanks` 8-bank stubs indexed by Country×Appearance, 10×10 each; seeded `NameGenerator.generate()` → `NamePair`). **Phase 4** persistence: `LegendEntry` + `LegendsArchive` resources + `SaveManager` autoload (registered in `project.godot`) persisting Player + Legends to `user://` `.tres`. Critical correctness point held + regression-tested: `archive_to_legends` **deep-duplicates** (`p.duplicate(true)`) so a loaded (path-bearing) Player is embedded **inline** (`sub_resource`), not as an `ext_resource` pointer to a file `clear_player()` immediately deletes — the spec reviewer empirically proved the guard catches the shallow-copy failure. Loads use `CACHE_MODE_IGNORE` (fresh disk read, no aliasing). **46 tests green, zero orphans**, 6 commits. Each task ran implementer → spec-compliance review → code-quality review (both gates passed). Two more latent plan-test bugs found + patched back into the plan: (1) `:=` type-inference through the untyped `var sm` receiver is a **parse error** → use `=`; (2) the test created an orphaned `Node` per run → `sm.free()` in `after_each`. |
| 2026-06-06 | Theme 7 started — `resolve_ball()` sim atom built + merged | First slice of the match engine (ADR 0004). Decomposed Theme 7 into 7a sim core → 7b Season wrapper → 7c balance harness, and the sim core itself bottom-up (ball → innings → match → Intent). Built the smallest rung: pure deterministic `BallResolver.resolve_ball()` — two-stage logistic (wicket: Composure vs Attack; runs: Power vs Control), Intent couples both, coefficients in a `BallTuning` resource (strawman, harness-swept later). Full brainstorm→spec→plan→inline-build→PR cycle; 16 new tests (97 green). Design sandbox `docs/mockups/ball-resolution-v1.html`. PR #3 merged to `main`. Neither open auto-sim question blocked the atom — they're innings/match-level. |
| 2026-06-07 | Theme 7a rung 4b — per-phase bowling rotation built + merged | Second half of rung 4 (PR #8): the bowling side gets its first decision lever, mirroring 4a Intent and realizing `setNextBowler` (ADR 0006). `BowlingPlan` (`enum Kind {PACE, SPIN}`, one type per phase, `for_over()`, factories; bare `new()` == textbook) + `BowlingAttack` (derives pace/spin `(attack,control)` from a base pair ± tilt const, floored at 1; `profile(kind)→Vector2i`). Threaded as optional trailing params through `simulate_innings()` (per-over profile) and `simulate_match()` (opt-in `player_bowling_plan`: null → no rotation, byte-identical to 4a; set → Player team bowling uses the plan routed to the opposition's batting innings, opposition bowls a fixed `textbook()` AI default). **Real EV trade (ADR 0003):** pace = more wickets + more runs, spin = containment — neither dominates. **Scope calls:** narrowed 4b to the rotation *mechanism* — **bowler husbanding (pool + 4-over cap + decay) deferred to optional 4c**; **Player stays bat-only**. Built inline test-first (4 tasks). 10 new tests (154, was 144): tilt+floor, phase mapping+factories, innings null==scalar + determinism + directional run-rate, match null==4a + routing + determinism. Spec `docs/superpowers/specs/2026-06-07-bowling-change-design.md`, plan `docs/superpowers/plans/2026-06-07-bowling-change.md`. **Match sim core (7a) now functionally complete; next = Season wrapper (7b).** |
| 2026-06-07 | Theme 7a rung 4a — per-phase batting Intent built + merged | Split rung 4 into **4a (Intent)** + **4b (Bowling Change)** — they're cleanly separable and very different in size; 4a is small, 4b is a chunky new subsystem. Built 4a (PR #7): the sim stops hardcoding `Intent.BALANCED` — a small `IntentPlan` value object (3 phase bands: Powerplay 1–6 / Middle 7–15 / Death 16–20; `for_over()` lookup; `balanced()` + `textbook()` factories; phase boundaries as constants `POWERPLAY_OVERS=6`/`DEATH_START_OVER=16`) is threaded as an optional trailing param through `simulate_innings()` and `simulate_match()`. `null` → all-BALANCED → byte-identical to rung 3 (all prior tests stay green). Routed to the **Player's batting innings only**; opposition stays Balanced (opponent batting intelligence = later rung). This is the sim's **first real decision input** — the lever the ADR 0003 naive-vs-optimal skill-gap metric measures; the Theme-6 swipe-card later writes into this same seam. Realizes the `setIntent` verb of ADR 0006 (data path only). Built **inline test-first** (not subagent-driven — 3 tiny pure-domain tasks, zero test-isolation surface). 11 new tests (144, was 133): phase mapping, factories, null==balanced regression, determinism-with-plan, directional coupling (aggressive both outscores **and** out-dies defensive), match routing both directions (via the first-innings-uses-initial-RNG isolation trick), match determinism. Full brainstorm (Nico chose "brainstorm then check in", then "resume AFK") → spec → plan → build → PR cycle. Spec `docs/superpowers/specs/2026-06-07-intent-plan-design.md`, plan `docs/superpowers/plans/2026-06-07-intent-plan.md`. |
| 2026-06-07 | Theme 7a rung 3 — full-match sim built + merged | Third sim rung (PR #6): `MatchResolver.simulate_match()` calls `simulate_innings()` twice + chase + cricket-standard result. Chose the **thinnest wrapper** (simplest-first): constant bowling, raw-int inputs, caller-supplied `player_bats_first` bool (toss deferred to 7b). **Settled the bowler-rotation open question as deferred** — constant bowling both sides; real per-over rotation lands at rung 4 (Bowling Change KM + opponent AI policy). Two backward-compatible extensions to `simulate_innings()`: optional statted Player (`null` → all-derived opposition innings) + optional chase `target` (stop the instant `total >= target`). `_decide_result()` extracted pure so the tie branch is testable on constructed innings. `MatchResult` holder (`Outcome` enum, margins, `margin_text()`). Full brainstorm→spec→plan→**subagent-driven** build (4 tasks, per-task spec + quality review + holistic final review)→PR cycle; 18 new tests (133 green, was 115). Determinism via shared rng consumed in fixed innings order. Spec `docs/superpowers/specs/2026-06-07-full-match-design.md`, plan `docs/superpowers/plans/2026-06-07-full-match-plan.md`. |
| 2026-06-07 | Theme 7a rung 2 — single-innings batting sim built + merged | Second sim rung: `InningsResolver.simulate_innings()` loops the ball atom into one deterministic T20 innings. **Settled the participation open question: build-driven batting position** — a batter-shaped build opens, a bowler-shaped build bats the tail; how many balls the Player faces emerges from order + strike rotation (chosen over fixed-quota; full team innings chosen over Player-only). 11 batters (Player statted + 10 derived from team strength), weakening tail, real strike rotation, 10-wkt/120-ball termination (fixes ball-spec §9 "no clock" gap). New `InningsTuning` strawman resource. Format confirmed T20/20-over from CONTEXT.md (not re-litigated). `InningsResult` holds aggregates only — no per-ball log (perf). Full brainstorm (w/ interactive innings sandbox `docs/mockups/innings-sim-v1.html`)→spec→plan→**subagent-driven** build→PR cycle; 18 new tests (115 green); even-contest avg ~123, inside the sane 110–175 T20 band. Plan had a self-caught off-by-one in two task test-counts. PR #5 merged to `main`. Also: project CLAUDE.md gained a "be concise" communication-style rule. |
| 2026-06-05 | Phases 8–10 done — lifecycle tests + ADR 0012; Player Creation build functionally complete | **Phase 8** (Manual retire) + **Phase 9** (Win-out) added `tests/unit/test_lifecycle_manager.gd` (3 tests) covering both end-state transitions already wired + live-verified in Phase 7: `manual_retire`/`win_out` → archive with the right `end_reason` (`retired`/`won`) + `career_ended` signal, plus the no-player no-op-warning path. These **retro-test existing Phase-7 code**, so they went green on first run (verification, not red→green TDD); the `LifecycleManager`/`LegendEntry`/`SaveManager` APIs matched the plan's assumed test verbatim (zero reconciliation). **Phase 10** wrote **ADR 0012** (Player lifecycle — hard permadeath: two triggers — Win-out + Manual retire — through a single `end_career(reason)`; auto-drop/age-based/relegation all deferred to Theme 9, form-anchored not Team-anchored when they land) and updated this roadmap. **Ran inline, not subagent-driven** — Nico flagged the per-phase stop/start as too granular, and these are tiny test-only/doc phases against code that already existed and passed live, so the subagent ceremony added little. **69 tests green, zero orphans**, 4 commits (`fa312a4`, `44bcb30`, `b9540bb` + roadmap). Only Phase 11.2 (final manual cold-start walk) remains; the automated sweep (Phase 11.1) is green. |

---

## Reference docs

- `CONTEXT.md` — domain glossary · source of truth for terminology
- `docs/adr/` — design decision records (0001 target market · 0002 V1 game shape · 0003 skill model · 0004 auto-sim architecture · 0005 Manager Boost no-menu · 0006 typed-effect palette · 0007 Joker authoring grammar · 0008 currency name + mark · 0009 Team durability · 0010 around-the-match navigation shell · 0011 global rank via Tour-distribution percentile · 0012 Player lifecycle hard-permadeath)
- `docs/joker-pool-v1.md` — all 45 Jokers authored (theme 4 content)
- `docs/superpowers/specs/` — implementation specs (`2026-06-01-player-creation-design.md` — Player Creation flow + permadeath lifecycle · `2026-06-02-hall-of-fame-design.md` — Hall of Fame / Legends-wall screen, replaces the build plan's HoF stub later · `2026-06-06-ball-resolution-design.md` · `2026-06-07-innings-sim-design.md` · `2026-06-07-full-match-design.md` · `2026-06-07-intent-plan-design.md` · `2026-06-07-bowling-change-design.md` · `2026-06-07-season-wrapper-7b1-design.md` · `2026-06-07-season-league-7b2a-design.md` — the match-sim rungs incl. 4a Intent, 4b bowling rotation, 7b-1 Team/stars/toss/match interface, 7b-2a league phase)
- `docs/superpowers/plans/2026-06-01-player-creation-plan.md` — reviewed 12-phase Godot build plan for Player Creation (**the build authority; next to execute**)
- `docs/PLAYER-CREATION-DECISIONS.md` — claude.ai hi-fi decisions log (Player Creation + Hall of Fame); see its reconciliation banner — V1 builds the spec §3.5 4-label model, not the doc's 9-label matrix
- `docs/PLAYER-CREATION-GODOT-BUILD-NOTES.md` — claude.ai engine-binding notes (data binds / transitions / palette tokens); design reference, *not* the build authority (the plan is)
- `docs/PLAYER-CREATION-HANDOFF.md` — outgoing brief sent to claude.ai (archival; its AUS `#FFCD00` value is stale — DESIGN_HANDOFF §16.2's `#A86E00` is canonical)
- `docs/brand/` — Tons (₸) currency mark · SVG assets and usage notes
- `docs/DESIGN_HANDOFF.md` — locked design system (chrome, portraits, KMs, IP rules, palette, fonts, motion, country system, hi-fi addendum §16, around-the-match design system §17)
- `docs/THEME-5-HANDOFF.md` — claude.ai mockup brief for theme 5 (archival; theme 5 now closed)
- `docs/design-handoff-from-claude-2026-05-31.md` — return handoff from claude.ai for theme 5 (Australia palette fix + Collection screen scope-add)
- `docs/RESEARCH_LAUNCH_PLAYBOOK.md` — full launch research (not on build critical path)
- `docs/mockups/ball-resolution-v1.html` — interactive single-ball sandbox (Theme 7a rung 1)
- `docs/mockups/innings-sim-v1.html` — interactive single-innings sandbox (Theme 7a rung 2: build-driven position, strike rotation, live scorecard + run-worm, avg-of-200 demonstration)
- `docs/mockups/star-winrate-v1.html` — **real-Godot-sim** win-rate-vs-star-gap chart (Theme 7b-1; data from `tools/season_preview.gd`; the rising S-curve proving stars→outcome; per-match dots + standard-deviation panel; first seed of the 7c balance harness)
- `docs/mockups/league-finish-v1.html` — **real-Godot-sim** playoff-qualification + average-finish vs Player ★ chart (Theme 7b-2a; data from `tools/league_preview.gd`; full league phase over random fields)
- `docs/mockups/match-screen-final.html` — locked in-Match design (4 frames)
- `docs/mockups/in-match-hi-fi-v1.html` — claude.ai hi-fi pass for in-Match
- `docs/mockups/around-the-match-v1.html` — claude.ai hi-fi pass for around-the-match screens (16 frames across 7 sections)
- `docs/mockups/player-creation-v2.html` — claude.ai hi-fi pass for Player Creation + Hall of Fame (**locked look**: portrait-anchored Identity · drag sliders · portrait-card Legend rows · gold retire badge)
- `docs/mockups/player-creation-v1.html` — Player Creation variants exploration (1A/1B/1C · 2A/2B · 3A/3B/3C; superseded by v2 for the locked directions)
- `docs/mockups/playable-prototype.html` — interactive 70-sec demo
- `docs/mockups/player-avatars-styles.html` — 3 art-style comparison
- `docs/mockups/the-t-mark-v1.html` — Tons (₸) currency-mark exploration

---

## Deferred (launch track — not blocking the build)

Marketing · TikTok/Insta devlog · soft launch playbook · Apple/Google submission · ad mediation · multi-language beyond English/Hindi essentials · battle pass / live ops · multiplayer

When ready, read `docs/RESEARCH_LAUNCH_PLAYBOOK.md`.

---

## Done so far

- Brainstorming session: in-match design (8 key-moment types defined; 3 fully designed)
- Player avatar system: Card Portrait + 3 layers (achievement / wear / identity) + split-bg evolution
- Hi-fi pass on claude.ai: stadium-scene CRR worm, jokers panel, water-meter boost, 10-nation palette
- Design handoff doc (full system spec)
- Engine decision (Godot)
- Launch playbook research saved (`docs/RESEARCH_LAUNCH_PLAYBOOK.md`)
- 7-theme roadmap (this doc)
- `grill-with-docs` skill installed for next session
- Scope + Game shape theme — V1 locked as a roguelite cricket-career
- `CONTEXT.md` domain glossary + ADRs 0001 (target market) & 0002 (V1 shape)
- Git repo initialised + pushed to GitHub (private: `github.com/nico101rsa/cricket-sim`)
- Gameplay & balance theme — auto-sim architecture (ADR 0004), skill model (ADR 0003), all 8 Key Moments designed, Manager Boost simplified (ADR 0005), 6-verb effect palette (ADR 0006)
- Portrait redesign — full-bg skill + facial-expression Form supersedes the split-bg model
- Currency naming + mark — **Tons (₸)**, two-bail T glyph (ADR 0008, `docs/brand/`)
- Jokers & meta loop theme **closed in full** — architecture (Shop cadence, authoring grammar ADR 0007, ~45-Joker pool across 6 archetypes, Carry-Over, slot 4 unlock, Offer cadence, Team durability ADR 0009) plus content (all 45 Jokers authored at `docs/joker-pool-v1.md` — 24 Common / 15 Rare / 6 Legendary, magnitudes V1 strawman pending balance harness).
- Around-the-match screens theme **closed in full** — Season hub, Pre-match, Shop ("Kit Room" scene with Kit Manager persona), Offer (with carry-over picker as footer), Result, Career Grid (diagonal-ladder topology), Career Records (global rank `#X / 2,112` computed analytically — ADR 0011) all designed in hi-fi (`docs/mockups/around-the-match-v1.html`). Navigation shell — Season hub as home + forced Shop/Offer interludes + no Main Menu — locked in ADR 0010. Australia palette fix (gold primary + green accent). **Onboarding closed** — Player Creation spec at `docs/superpowers/specs/2026-06-01-player-creation-design.md`: 2-screen Identity → Build flow, hard-permadeath lifecycle (Win + Manual retire), 4 Appearance buckets per Country, cricket-pun name gen, 20-point attribute slider, live classifier label. Settings still deferred.
- **Player Creation build — Phases 0–7 done** (branch `player-creation-build`, 2026-06-03/04): Godot 4.6.3 project + GUT 9.6 test harness, 8 tested domain types, classifier (Phases 0–2); name generator — `NameBanks` + seeded `NameGenerator` (Phase 3); persistence — `LegendEntry`/`LegendsArchive` + `SaveManager` autoload with inline deep-duplicate archive + fresh-read loads (Phase 4); **first UI — `AppearancePicker` subscene + `Identity` screen (country/city/appearance/name + re-roll, Back-nav hydration), Phase 5**; **Screen 2 `Build` — 4 attribute sliders + live points counter + live classifier label + identity recap + Confirm that finalises & persists the `Player`, Phase 6**; **flow wiring — `LifecycleManager` autoload (single "end Career" owner) + 3 stub scenes + `main.tscn` router (cold-start → Creation → stubs; existing save → Season hub; `career_ended` → Hall of Fame), Phase 7**. **66 tests green, zero orphans**, 33 commits, test-first throughout. Phases 3–7 ran subagent-driven (implementer → spec review → code-quality review). Five latent plan bugs found + patched in earlier phases; Phase 5 added the invisible-UI lesson (flat Button + `modulate` = invisible; use `StyleBoxFlat`); Phase 7's eyeball walked the **whole flow end-to-end in a real window** (renders + routes correctly, retire/dev-win archive to Hall of Fame, LegendsArchive persisted across a window restart) and cleared the benign `main.tscn` load warning. The full Player Creation flow is now runnable end-to-end (skeleton/unstyled by design — visual polish is Theme 6); next is Phase 8 (manual-retire automated tests). Project `CLAUDE.md` holds the Godot dev conventions.

---

## Notes

- **Seeing games:** the sim is headless with no per-ball log, so games can be shown as a **scorecard** (a throwaway runner through the real `simulate_match()` does this on demand — ask "show me a game"). To *watch a game unfold ball-by-ball* would need an optional **delivery-log** addition to the sim — a small future rung, natural fit alongside the Theme 6 in-match screen.
- "Successful" = I play it and enjoy it. Anything beyond is bonus.
- Playtest threads through every theme — budget time for it; fun doesn't emerge from spec docs.
- If a feature would take >1 weekend to design AND build, decide V1 vs V2 first.
- **Balance harness** is the tuning workhorse — pure Python or Godot-headless module that runs thousands of Seasons over a parameter grid, measures the optimal-vs-naive skill gap (ADR 0003), and flags overpowered Jokers / underpowered Tour cells. Lives in the Tech foundation theme but every numerical decision from theme 3 onward routes through it. The auto-sim's headless / deterministic-seedable design (ADR 0004) exists precisely to enable this.
