# Cricket Sim · Project Roadmap

Mobile **roguelite cricket-career** mashing **Reigns × Balatro × management**, targeting South Africa & Australia. ~2:45 per match. Personal/learning project — "success" = I play it and enjoy it.

---

## Current status

**Phase:** Build · **Theme 7 (Tech foundation → match sim → balance harness) on `main`.** Player Creation V1 + Hall of Fame merged (PRs #1/#2). **Season loop complete** (PRs #3–#13): ball → innings → match → Intent/bowling → Team/stars/toss → league table → playoffs → outcome. **Theme 7c (balance harness): A platform (PR #14) + B skills/Player-as-bowler (PR #16) + D5 stat line (PR #17) + C jokers-in-sim (PRs #18–#26: all 45 wired & swept) + ⚖️ BUILD-BALANCE — Slices 1/1b/2/3 (PRs #27–#30) → BUILD BALANCE LOCKED + ALL-ROUNDER VIABILITY Part 1 (PR #31: 4-over participation + exact float conservation).** **363 tests green.** **⚖️ FAIR-FIGHT BASELINE (rung 1, PR #32) + JOKER RE-TUNE (rung 2, 2026-06-10) both done.** The sim is now a fair fight (no-joker even-★3 = 48.1%) **and** the 45 jokers are tuned to rarity bands against that honest floor: every over-strong joker pulled in (Review Master +28.4%→+9.6%, Snicko +13.6%→+4.9%, Captain's Call +9.8%→+5.9%), Carry Your Bat bug fixed (−2.9%→+4.6%), no joker negative or an auto-win. **Big finding:** conditional jokers (4/6 Legendaries + many Form/change-triggered ones) read below band by **fire-rate, not magnitude** — then the **scenario-sweep rung (rung 3, 2026-06-10)** measured them honestly, and a **mechanic-change rung (2026-06-10)** fixed the two magnitude-capped jokers (Chase Master + composure, Wicket Maiden + economy → both in band; see Last closed). **366 tests green.**
**Last closed:** **Capped-joker mechanic changes** (2026-06-10, branch `capped-joker-mechanic-changes`, PR pending). Resolved rung 3's two "magnitude-capped" residuals by giving each a **second component that converts in the common case** (not a bigger first number) — both via the existing duplicate-id multi-row pattern, no new primitive. **The Chase Master** gains a **composure/survival** row (`wicket ×0.62`, same chasing+Aggressive gate): runs saturate vs the chase win-ceiling, but *not getting out* wins close chases → realized **+3.6% → +6.6%** (Legendary floor), no blowout. **Wicket Maiden** gains an **economy/dot-pressure** row (`runs ×0.62` over the 6-ball post-wicket window): conceding fewer runs always converts (extra wicket-chance only pays if a wicket falls) → **+1.0% → +4.0%** (Rare floor); thematically a wicket-maiden = wicket + no runs. **Takeaway:** a saturated/rare-trigger mechanic is fixed by a *converting* second component, not a bigger magnitude. **366 tests green.** Spec `docs/superpowers/specs/2026-06-10-capped-joker-mechanic-changes-design.md`. Pool doc + viewer DATA refreshed.
**Prior:** **Scenario sweep — honest measurement of conditional & synergy jokers** (rung 3, 2026-06-10, branch `scenario-sweep-conditional-jokers`, PR #34). Closes the rung-2 participation residual: a **chase profile** (Player forced to bat 2nd via a new off-by-default `force_player_bats_first` toss seam) + **designed-combo arms** measure conditional jokers in contexts that fire them. **Headline:** The Chase Master is a *genuine* Legendary when it fires — dormant **+3.2%** standard → **+7.2% in-condition** (rung 2 only saw the dormant number). **Buffs** (uncontrollable, under-band): Chase Master ×1.20→**1.40**, Match-Winner's Vigil ×0.80→**0.62** (in-combo +2.7→+4.9%), Wicket Maiden ×1.30→**1.45**. **Key finding (refines DS2):** "buff to realized-band" works only where magnitude has headroom — Chase Master's runs-mult **saturates against the chase win-ceiling** (1.20→1.70 moved only +7.0→+7.9%, runs past the target are wasted) and Wicket Maiden's emergent trigger is **participation-capped**; neither reaches band by magnitude alone (needs a mechanic change — flagged for Nico). **Fire-rate (rung-D pricing input):** Chase Master fires ~**50%** (the toss; realized ~+3.6%). Form sources earn their slots (+5.3% marginal). **366 tests green** (+3, the toss seam). Spec `…2026-06-10-scenario-sweep-conditional-jokers-design.md` §10; plan `docs/superpowers/plans/2026-06-10-scenario-sweep-conditional-jokers.md`.
**Prior:** **Joker re-tune to rarity bands** (rung 2, 2026-06-10, branch `joker-retune-rarity-bands`, PR #33). Pure-tuning rung, inline TDD against the fair-fight floor — catalog magnitudes + DRS scalar dials, **no new gameplay mechanics**. **DB3 fallback applied:** the DRS "retain on fail" mechanic (Captain's Call / Review Master) gave effectively *infinite* reviews with no scalar to tune, so it was retired in favour of **bounded extra-review grants** (Captain's Call +2, Review Master +2); base retain-on-success (real T20 rule) kept, so the in-band DRS Commons (Cool Head/Spare/Captain's Eye) were untouched. Key dials: `DRS_MASTER_BONUS` 0.30→0.06, Snicko accuracy 0.25→0.12; Powerplay/Field 1.10/1.12→1.05, Death-Over 0.80→0.83, Power Surge `AMPLIFY` 1.20→1.40. **Findings + residuals table:** spec `…2026-06-09-joker-retune-rarity-bands-design.md` §10; plan `docs/superpowers/plans/2026-06-10-joker-retune-rarity-bands.md`. Pool magnitudes (`docs/joker-pool-v1.md`) + viewer DATA refreshed. **363 tests green** (+1).
**Prior:** **Fair-fight baseline — symmetric opponent captain tools** (2026-06-09, PR #32). Re-sweeping the jokers (handoff option B) showed the no-joker baseline won ~62% not because of jokers but because the sweep gave the *Player* a game-plan + captain tools while the opponent played passively — a dishonest baseline to tune jokers against. With Nico, this grew into making the sim a **fair fight**, split into rung 1 (this, the baseline) + rung 2 (the joker re-tune). **Built (DF1–DF6, inline TDD):** Player DRS now reviews **any** team dismissal (was hero-only) + **2 reviews** (T20 rule); the **opponent** gets its own base boost + DRS via a **second `JokerRuntime` (empty jokers)** in `simulate_innings` — side-aware buffs + a **unified DRS block** where both sides survive a wicket / claim a dot (gated on the *original* outcome so no review "tennis"); symmetric opponent batting intent + a **run-margin readout** in the sweep. **A build-time probe attributed the residual:** no-DRS-either-side = 50.3% (the statted hero is neutral), so the gap above 50% was purely the Player's claim-review edge → folding in the opponent claim-review (initially deferred, cheap) took the baseline **53.7% → 48.1%** (margin −1.99 runs), hitting the ~48–50% even-contest target. Boost is already team-wide on both sides (verified, no change). **362 tests green** (+6). Spec `docs/superpowers/specs/2026-06-09-fair-fight-baseline-design.md`, plan `docs/superpowers/plans/2026-06-09-fair-fight-baseline.md`. **Deferred (self-play):** opponent boost water-meter fidelity, opponent jokers/decision-AI.
**Prior:** **All-rounder viability Part 1 — 4-over participation + exact bowling conservation** (2026-06-09, PR #31). Nico flagged the all-rounder as a likely trap build (it under-bowls and earns fewer Tons than a specialist). Brainstormed → spec `docs/superpowers/specs/2026-06-09-allrounder-viability-and-team-construction-design.md` (decisions DA1–DA5). **Part 1 (built):** the all-rounder now bowls its **full 4 overs** (overs curve `bowl_overs_gain` 8→13, `base` -2.5→-2.6 → batter 0, 7/7 1, 6/6 3, all-rounder 4, bowlers 4 — DA5: batting-ish builds bowl *somewhat*). Letting weak builds bowl exposed a dip (integer bowling-conservation *under-compensated* a weak part-timer → team bowled below par), fixed by making **`_conserved_bowling` return an exact float** (widened bowling-stat params `int→float` down `simulate_match → simulate_innings → resolve_ball`; int callers coerce so the scalar path is byte-identical, all joker tests untouched); `bowl_concentration_k` re-tuned 1.0→0.5 (exact conservation is tighter). **Win-rate flat — every build 46.9–48.9% (win ≈ loss), spread 2.0 (was 4.1).** **Key empirical finding:** "fair" = win ≈ loss ≈ ~48–49%, NOT 50% (ties take ~1% off the top; a build winning 50% would actually be winning more than it loses). Rating dials unchanged; the net-runs rating stays an honest "smile" (specialists rate above the all-rounder) — that's by design. Inline TDD; **356 tests green** (+2). Plan `docs/superpowers/plans/2026-06-09-allrounder-4over-participation.md`; calibrated baseline spec §10. **Parts 2 & 3 = recorded decisions, NOT built** (see Next session).
**Prior:** **Build-balance Slice 3 — gap-fill + bowling conservation → balance LOCKED** (2026-06-09, PR #30). The balance payoff: **every Player build now wins ~47–49% at even ★3** (was a 45→59% U where the bowler was the best build). **Win-rate spread cut 13.7 → 4.1 — D1 (build is win-neutral) MET.** Two budget-conservation mechanisms completed what Slice 2's D6/D7 deferred (decisions §8.6 D11–D14): **(1) batting gap-fill** — `Team.build_xi` tops up/docks the 10 teammates so team batting always totals the standard **122** whatever the build (distributed top-first = high-leverage; lifts the all-rounder/mid dip); **(2) bowling conservation** — `MatchResolver._conserved_bowling` scales the non-Player overs down so the team's 20-over bowling total stays at `over_limit·S`, removing the bowler-Player's *free* 4-over lever (a new `InningsTuning.bowl_concentration_k=1.0` dial charges a concentrated strong spell extra, since 4 overs at attack-8 take convexly more wickets than the linear budget — integer-rounding of the conserved scalar is the source of the residual ±~2). **Key insight that drove it:** the roster (Slice 2) feeds *batting only* — bowling was purely scalar, so gap-fill on the order alone could never touch the bowler peak; conservation had to be added on the bowling side. Conservation lives **only in `simulate_match_teams`** → the scalar `simulate_match` path is byte-identical, so every joker test is untouched. **Rating re-solved (D13):** par re-derived from the generic build's *new* output (`sr_par` 111.6→**107.4**, `rr_par` 6.4→**9.0**) so the average player rates ~0; `wicket_value` 10→**2.0** solved so a pure batter (9.1) ≈ pure bowler (9.6). **Honest finding (one open call for Nico, non-blocking):** the *individual* net-runs rating is a **"smile," not flat** — specialists concentrate measurable surplus, the all-rounder spreads it thin → it sits in a shallow valley (~0.3). No linear dial flattens this (it's net-runs being honest); the all-rounder's offsetting value is flexibility/joker-fit, which the neutral sweep excludes (D5). Spread 9.3 is inside the spec's ±10 rating band. Team-fairness (the headline) is fully solved; making the all-rounder *individually* equal would need a model change (crediting option-value) — a separate rung. Inline TDD; **353 tests green** (+6). Plan `docs/superpowers/plans/2026-06-09-build-balance-slice3-gapfill.md`; decisions spec §8.6, calibrated baseline §9.5.3.
**Prior slice:** **Build-balance Slice 2 — discrete real roster** (2026-06-08, PR #29). Replaced the fake "every teammate is one cloned ~5 strength number" placeholder with a **real XI of 11 individual players**, each a 20-point `Attributes` build from archetypes (BATTER 8/8/2/2 · BOWLER 2/2/8/8 · ALLROUNDER 5/5/5/5). **The seams:** `Team.archetype_*()` + `Team.standard_xi()` (6 BATTER · 1 ALLROUNDER · 4 BOWLER → 122/98 batting/bowling point split) + `Team.build_xi(player_attrs, ppos)` (Player replaces the archetype at their build-driven batting position); `InningsResolver._build_batters` gained a **real-roster path** (each slot reads its own power/composure, a uniform star-offset shifts the whole card, Player found by reference identity, synthetic tail curve dropped) selected by an optional `batting_roster` param — **empty roster = byte-identical old clone path, so every scalar caller/test is untouched**; threaded `batting_roster`/`team_bat_offset` through `simulate_innings` → `simulate_match` → `simulate_match_teams` (which assembles both XIs). **Key design (spec §8.5, D6–D10):** star-directionality (5★ beats 0.5★) preserved by applying the *existing* star-derived `batting_strength` as a **uniform offset** to each roster member (~0 at even ★3 so the balance baseline is the pristine archetypes; `batting_strength`/`bowling_strength` themselves untouched → League/Season safe); bowling stays scalar (per-bowler husbanding still deferred to a future 4c). **The re-grounding INVERTED the win-rate shape** (spec §9.5.2): pre-roster the batter was the high (64%); now the **bowler build is the high (59%)**, the batter sits fair (48.7%), with a U-valley at mid/all-rounder builds (~45%) — strong real top-orders make a batter-Player a marginal lever while a bowler-Player adds a real 4-over lever; Player wkts/match collapsed (0.85 vs old 1.90, which defused the wicket_value over-rating) and ratings tightened to [−3, +8]. **Neither balance bar is flat yet — that's Slice 3.** One re-grounded side-effect: a Form-window joker win-rate test fell below its N=300 signal floor (Player is now a smaller lever) — widened to a 1.5×/6-ball window at N=600, same mechanic. Inline TDD (5 tasks); **347 tests green** (+12). Plan `docs/superpowers/plans/2026-06-08-build-balance-slice2-discrete-roster.md`; spec decisions §8.5 + baseline §9.5.2.
**Prior slice:** **Build-balance Slice 1 (net-runs Player rating) + Slice 1b (wicket-rate fix)** (2026-06-08, PRs #27/#28). Nico re-engaged on balance with a clear goal: an all-rounder should be *as good as* a batter or bowler — balanced at the **team** level (every build ~50% win-rate at even ★3) and the **individual** level (equally valuable, measured in one currency). Slice 1 built that currency: **`PlayerRating.rate()`** scores any build in **net runs** = runs added with the bat (`runs − sr_par/100·balls`, so strike-rate falls out) + runs saved with the ball (`rr_par/6·balls − conceded + wickets·wicket_value`); `RatingTuning` holds the dials (`sr_par`/`rr_par` *derived* from the generic 5/5/5/5 build so an average player rates ~0; `wicket_value=10` strawman). The sweep (`tools/build_spectrum_sweep.gd`) now prints rating per build. **The rating immediately earned its keep — it exposed a sim bug win% alone hid:** a 4-over specialist was taking **~3.17 wkts/match** (unreal; real elite ≈ 1.7–2.0) which ballooned its rating to ~38 vs a batter's ~11. Slice 1b fixed it via **`BallTuning.k_w` 0.42→0.24** — a *twofer*, since the same attack-vs-composure gain also made an 8-composure batter near-immortal: the specialist now takes ~1.9 wkts and the batting average fell from **141 → 86** (`base_w` / even-contest 3.5% baseline untouched, so no tests broke). Win-rates compressed (67/52/62 → 64/51/58) but **flattening to ~50 is Slice 3's job** (team gap-fill), not the ball dial's. Spec `docs/superpowers/specs/2026-06-08-build-balance-rating-and-team-design.md` (D1–D5 + findings §9.5/§9.5.1); plan `…/plans/2026-06-08-build-balance-slice1-player-rating.md`. Inline TDD; **335 tests green** (+5). **Decision (D2):** rating currency = net runs (Nico's call); **(D3)** team model = discrete real roster, every player a 20-pt build (team = 220); **(D5)** sweep stays neutral-Intent.
**Prior slice:** **Jokers in the sim — C2a: field-mode condition + state-gated jokers** (2026-06-08, PR #19). First sub-slice of **C2**. Auditing the 45-joker pool showed C2 is not one rung — the 40 remaining jokers need **six distinct new sim subsystems**, so **C2 is decomposed C2a→C2g** (one subsystem + its jokers per rung, mirroring 4a/4b and 7b-1/7b-2). C2a adds **field mode** + the jokers it unlocks, taking the implemented pool **5 → 11 of 45**. **Key call (D2):** field mode is a *readable captain condition jokers gate on* — **no intrinsic roll EV of its own this rung** (deferred to a future "field as a real lever" rung); layer C wires joker effects, not new gameplay levers. **The pieces:** `FieldPlan` (`scripts/data/field_plan.gd`, per-phase NEUTRAL/CATCHING/DEFENSIVE, mirror of `IntentPlan`/`BowlingPlan`); `JokerEffect.field_req` + extended `matches(..., field_mode := NEUTRAL)`; `JokerResolver.roll_mults(..., field_mode := NEUTRAL)`; `field_plan` threaded through `simulate_innings` + `simulate_match`/`simulate_match_teams`, **routed to the opposition's batting innings** (Player sets the field while bowling); no new RNG draws → determinism kept. **Multi-buff jokers** (one joker bending both rolls) via **duplicate-`id` rows** (no model change) — the resolver already products them; `JokerCatalog.implemented_groups()` groups them (`{id,jname,rarity,effects}`), `implemented()` flattens. 6 new jokers (verbatim magnitudes): #4 Rotate the Strike, #6 Carry Your Bat, #12 Slog Over Specialist (batting); #16 Tight Lines, #20 Dot Ball Pressure, #23 Cordon Killer (bowling, field-gated). **The data** (`tools/sweep_jokers.gd`, 2000 even ★3/arm, shared intent + shared field plan, 14 arms): new field jokers Dot Ball Pressure +4.6% / Tight Lines +3.6% / Cordon Killer +1.3%, Slog Over Specialist +4.6%, field-defensive stack +6.6%, batting stack +10.2%; the original 5 reproduce PR #18 exactly (regression sanity). **Note:** Rotate the Strike reads +0.0% in this sweep because the shared intent plan never sits in Balanced (effect covered by the unit test). Inline test-first (5 tasks); 21 new tests (240, was 219). Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC2a-field-design.md`, plan `docs/superpowers/plans/2026-06-08-jokers-in-sim-7cC2a-field.md`. **Known:** pre-existing HoF scene-test orphans unchanged.
**Prior slice:** **Jokers in the sim — vertical slice (7c-C)** (2026-06-08, PR #18). First slice of harness **layer C**: the reusable per-ball **effect-application seam** + 5 representative Jokers wired end-to-end (catalog → sim → sweep → viewer), proving the pattern before scaling. **The seam:** almost every joker bends one of `resolve_ball`'s two rolls (`p_wicket`, scoring strength `s`), so an active-joker layer multiplies them per ball. `JokerEffect` (`scripts/data/joker_effect.gd`) pure data tuple — `side`/`target`/`mult`/`intent_req`/`ball_min`/`ball_max` + `matches()` (fields stored as `int`, not local enum types — GDScript can't unify a class-local enum type across the call boundary). `JokerResolver.roll_mults()` (`scripts/domain/joker_resolver.gd`) returns `Vector2(wicket_mult, runs_mult)`. Threaded as two params on `resolve_ball` + optional `jokers`/`player_is_batting` through `simulate_innings`/`simulate_match(_teams)`. `JokerCatalog.slice_v1()` authored the 5. 22 new tests (was 197). Spec `…-7cC-slice-design.md`, plan `…-7cC-slice.md`.
**Prior slice:** **Player-as-bowler — build-driven bowling overs** (2026-06-08, PR #16). Closes the 7c-1 "bowling build is inert" finding *with the fix*, completing **harness layer B (skills)**. The Player's bowling Attributes (Attack/Control) finally feed the sim — the exact mirror of build-driven batting position: a bowling-heavy build bats lower **and** bowls more overs. New `InningsResolver.player_overs(attrs, itun)` maps the same batting/bowling **share** that drives position → a **0–4** over quota (strawman curve `bowl_max_overs`/`bowl_overs_gain`/`bowl_overs_base` in `InningsTuning`, harness-tunable; batter 0 / balanced 2 / specialist 4), and `player_bowling_overs(n, total)` picks the overs evenly spaced. Threaded as three off-by-default trailing params through `simulate_innings` (on a Player over the batter faces the **Player's** Attack/Control, overriding the team scalar / pace-spin profile) and computed-from-the-real-Player in `simulate_match` (into the opposition's batting innings; no new RNG draws → determinism preserved). **The lever moved:** re-running the skills sweep (`tools/sweep_skills.gd`), a **bowling build's win-rate jumped 48% → 62%** — it still barely *bats* (avg ~3 runs) but now wins through *bowling*; at even team strength batting (67%) and bowling (62%) builds win about equally — no dominant build (ADR 0003 holds). **Deliberate scope call (spec D1):** this **intentionally breaks the byte-identical-core invariant** held since rung 3 — opposition outcomes now shift for any non-pure-batting Player (the point: bowling becomes real); one pre-existing snapshot test's baseline was updated to account for the Player bowling their own overs, all directional/determinism tests stand. Inline test-first (4 tasks); 10 new tests (194, was 184): quota-map (batter/balanced/bowler/monotonic/over-set), `simulate_innings` off-by-default + strong-bowler-concedes-fewer + determinism, `simulate_match` win-rate-lever (the test that *would have failed* pre-rung) + determinism. Eyeball: refreshed `docs/mockups/distribution-viewer-v1.html` (runs curves unchanged — a bowler barely bats — but the win-rate column tells the new story). Spec `docs/superpowers/specs/2026-06-08-player-as-bowler-design.md`, plan `docs/superpowers/plans/2026-06-08-player-as-bowler.md`. **Known:** pre-existing HoF scene-test orphans unchanged.
**Prior slice:** **Theme 7c rung 7c-1 — the balance harness platform** (2026-06-08, PR #14). Layer **A** of the 7c decomposition (A platform → B skills → C jokers-in-sim → D ₸ economy → E self-play AI). The reusable lens for "run 1000s of scenarios + see the outcome distribution shift before/after." Three tested pieces in a new `scripts/harness/` library: **`Distribution`** (mean/sd/min/max + histogram over a list of numbers, pure) and **`Sweep`** (`run(arms, n, scenario: Callable, base_seed)` drives a model-agnostic scenario over `arms × n` with **paired seeds across arms** so arm-to-arm differences are the config's effect not RNG noise; `values_of` extracts a field), plus a **reusable overlaid-histogram viewer** `docs/mockups/distribution-viewer-v1.html` (swap `DATA` for any sweep). **Demo (skills):** `tools/sweep_skills.gd` varies the Player's Attribute build over even ★3 vs ★3 matches → player-runs distribution + win-rate: batting build (8/8) **43.7 mean runs / 67% win**, balanced **13.1 / 51%**, bowling build (2/2) **2.7 / 48%**. **Finding surfaced as data:** a bowling build is *no better than balanced* (~48%) — the Player's Attack/Control don't feed the sim yet (Player-as-bowler deferred), so they're an **inert lever today**. Inline test-first (4 tasks); 7 new tests (184, was 177): Distribution stats/empty/histogram, Sweep shape/values_of/paired-seeds/determinism. Spec `docs/superpowers/specs/2026-06-08-balance-harness-platform-7c1-design.md`, plan `docs/superpowers/plans/2026-06-08-balance-harness-platform-7c1.md`. **Known:** pre-existing HoF scene-test orphans unchanged.
**Prior slice:** **Theme 7b rung 7b-2b — the Season playoffs + outcome + ★ mutation** (2026-06-08, PR #13). Completes the Season loop on top of 7b-2a. Three pieces: **`Team.mutate_stars(rng)`** (`scripts/data/team.gd` — ADR 0009 Season-rollover Markov mutation: ±0.5 ~30% / ±1.0 ~5% / no-change ~65%, clamped 0.5–5.0; a built+tested *primitive*, not yet wired into a multi-Season loop), **held strengths exposed on `LeagueResult`** (`team_bat`/`team_bowl` — so playoffs reuse the *same* per-Season strengths the league drew, no re-deriving), and **`SeasonResolver.simulate_season(...)`** (`scripts/domain/season_resolver.gd` → `SeasonResult`). `simulate_season` runs the league (7b-2a) then the **top-4 knockout playoffs** — two semis (seed 1v4, 2v3) → **The Final** (winners → 1st/2nd) + **3rd-place playoff** (losers → 3rd/4th) — reusing held strengths (Player statted when involved via the `_knockout` slot-ordering helper; ties → higher seed advances), assembles the final 1–8 ordering, and flags **`beat`** (top 3) + **`won_final`** (1st). `SeasonResult` holds the `LeagueResult`, the 4 playoff `MatchResult`s, `final_order`, `player_final_position`, `beat`, `won_final`. **Eyeball:** `tools/season_outcome_preview.gd` → `docs/mockups/season-outcome-v1.html` charts **beat-rate + win-Final-rate vs Player ★** — both climb with ★, beat ≥ win by construction; notable emergent property: even a 5★ team only wins the Final ~55% (the championship is two knockout coin-flips). **Scope calls (documented in spec):** playoffs+outcome+mutation in one rung; knockout tie → higher seed advances; `won_final` is raw championship (the "win the Level" Premium-tour gating + the multi-Season Career loop that applies `mutate_stars` are deferred to the **Career rung**). Inline test-first (4 tasks). 8 new tests (177, was 169): mutation distribution+clamp, held-strengths exposed, season determinism + final-order-permutation + outcome-consistency + champion-via-Final + directional. Spec `docs/superpowers/specs/2026-06-08-season-playoffs-7b2b-design.md`, plan `docs/superpowers/plans/2026-06-08-season-playoffs-7b2b.md`. **Known:** pre-existing HoF scene-test orphans unchanged.
**Prior slice:** **Theme 7b rung 7b-2a — the Season league phase** (2026-06-07, PR #12). Loops 7b-1's match into an **8-team single round-robin** (28 games, each team plays 7) and produces a real standings table + the Player's finishing position. Three new pieces: **`StandingsRow`** (`scripts/data/standings_row.gd` — a team's league record: points + runs/balls for & against, with `nrr()` Net Run Rate, actual-balls V1 simplification of the all-out quota rule), **`LeagueResolver`** (`scripts/domain/league_resolver.gd` — `round_robin(n)` fixtures + `simulate_league(...)`), and **`LeagueResult`** (`scripts/data/league_result.gd` — ranked `standings`, `player_position` 1–8, `made_playoffs` = top 4, `player_matches`). `simulate_league` derives each Team's strength **once per Season** (held all Season, ADR 0009 — the "noise hoist"), simulates **all 28 games** (Player games statted via `simulate_match`; non-Player games via the already-supported `simulate_match(null, …)` derived path — one code path, a genuine table), accumulates points (win 2/tie 1/loss 0) + NRR, and ranks **points → NRR → index**. **Eyeball deliverable:** `tools/league_preview.gd` runs the real league phase sweeping the Player team's ★ against a **random** 7-opponent field; `docs/mockups/league-finish-v1.html` charts **playoff-qualification rate + average finish vs ★** — both climb with ★, qualification crosses 50% at ~★3.5 (the mid-curve step is the natural sigmoid of a hard top-4 cut on 8 teams). **Scope call (documented in spec):** split 7b-2 into **7b-2a (league phase, this)** + **7b-2b (playoffs + beat/win outcome + ★ Markov mutation)**, mirroring the 4a/4b + 7b-1/7b-2 splits. Built **inline test-first** (4 tasks). 6 new tests (169, was 163): round_robin coverage; NRR sign/zero; league determinism + structural integrity + table-sorted + directional (5★ qualifies more + finishes higher than 0.5★). Spec `docs/superpowers/specs/2026-06-07-season-league-7b2a-design.md`, plan `docs/superpowers/plans/2026-06-07-season-league-7b2a.md`. **Known:** pre-existing HoF scene-test orphans unchanged (added only pure domain logic + diagnostics).
**Prior slice:** **Theme 7b rung 7b-1 — Team · stars→numbers · toss · Team-bundled match interface** (2026-06-07, PR #9). Also a follow-up **harness-driven balance tune** (PR #11): Tour `spread` 3.0 → 1.5 (and made `float`) to flatten the underdog drop-off after Nico flagged it too steep — a 2-tier underdog now wins ~17% (was ~3%). The sim finally speaks the game's real vocabulary — ★-rated Teams, a Tour that sets strength, a simulated toss — layered on top of the **byte-identical** `simulate_match()` core (same additive discipline as 4a/4b). Three new pieces: **`TourDistribution`** (`scripts/data/tour_distribution.gd` — a Tour's strength band `mean ± spread`; `percentile(frac)` maps a `stars/5` fraction → a strength number on the ~1–8 attribute scale; strawman mean 5 / spread 3 / noise 1, harness-tuned later), **`Team`** (`scripts/data/team.gd` — a ★-rated side 0.5–5.0; `batting_strength()`/`bowling_strength()` derive per-Match from `tour.percentile(stars/5) + noise`, two independent draws floored at 1, per ADR 0009), and the **toss** (`MatchResolver._resolve_toss()` — seeded 50/50 coin-flip replacing the caller-supplied `player_bats_first` bool). New entry point **`MatchResolver.simulate_match_teams(player, playerTeam, oppTeam, tour, …)`** derives the six strength ints + flips the toss (fixed RNG draw order: toss → 4 strength draws → core sim) and delegates to the existing core (not modified). **Eyeball deliverable** (Nico's "learn by seeing" ask): `tools/season_preview.gd` runs the **real** sim across star matchups, `docs/mockups/star-winrate-v1.html` charts **win-rate vs star gap** — a clean S-curve crossing ~54% at gap 0 (the slight edge is the statted Player on the Player's side), 0% at −4.5, 100% at +4.5. First concrete seed of the 7c balance harness. **Scope call (documented in spec):** split 7b into **7b-1 (this — play one Match between two Teams)** + **7b-2 (the Season league loop)**, mirroring the 4a/4b split. Built **inline test-first** (4 tasks: TourDistribution, Team, toss+wrapper, runner+chart). 9 new tests (163, was 154): percentile endpoints/monotonic/clamp; star→strength direction + noise bound/floor + zero-noise==percentile; match determinism + directional (5★ beats 0.5★ >80%) + even-teams balance. Also added `.claude/launch.json` (static preview server for `docs/mockups`). Spec `docs/superpowers/specs/2026-06-07-season-wrapper-7b1-design.md`, plan `docs/superpowers/plans/2026-06-07-season-wrapper-7b1.md`. **Known:** pre-existing HoF scene-test orphans unchanged (7b-1 added only pure domain logic + diagnostics).
**Prior slice:** **Theme 7a rung 4b — per-phase bowling rotation** (2026-06-07, PR #8). The bowling side gets its first decision lever — the mirror of 4a Intent, and the sim realization of `setNextBowler` (ADR 0006). Two new value objects: **`BowlingPlan`** (`scripts/data/bowling_plan.gd` — `enum Kind {PACE, SPIN}`, one type per phase 1–6/7–15/16–20, `for_over()`, `textbook()`/`pace_only()`/`spin_only()` factories; bare `new()` == textbook pace/spin/pace) and **`BowlingAttack`** (`scripts/data/bowling_attack.gd` — derives pace/spin `(attack, control)` profiles from a base pair ± a tilt constant, default 2, floored at 1; `profile(kind) → Vector2i`). Threaded as optional trailing params through `simulate_innings()` (per-over profile via `balls / 6 + 1`; null → constant scalar pair) and `simulate_match()` (opt-in `player_bowling_plan`: null → no rotation either side, byte-identical to 4a; set → Player's team bowling uses the plan, routed to the innings where the opposition bats, opposition bowls a fixed `textbook()` AI default). **The decision is a real EV trade (ADR 0003):** pace = attack-tilted (more wickets **and** more runs); spin = control-tilted (contains, threatens less) — pace for wickets, spin for containment, neither dominates. **Scope calls (documented in spec):** narrowed 4b to the rotation *mechanism* — **bowler husbanding (finite pool, 4-over cap, fatigue/decay) deferred to a possible 4c**; **Player stays bat-only** (team bowling from team strength; Player-as-bowler deferred). Built **inline test-first** (4 tiny pure-domain tasks, zero isolation surface). 10 new tests (154, was 144): BowlingAttack tilt + floor; BowlingPlan phase mapping + factories; innings null==scalar regression + determinism + directional run-rate (pace more wickets at higher run rate than spin over 40 innings); match null==4a regression + routing (first-innings isolation trick) + determinism. Spec `docs/superpowers/specs/2026-06-07-bowling-change-design.md`, plan `docs/superpowers/plans/2026-06-07-bowling-change.md`. **Known:** pre-existing HoF scene-test orphans unchanged (4b added only pure domain logic).
**Prior slice:** **Theme 7a rung 4a — per-phase batting Intent** (2026-06-07, PR #7). The sim stops hardcoding `Intent.BALANCED`: a small `IntentPlan` value object (`scripts/data/intent_plan.gd`) holds one Intent band per phase (Powerplay 1–6 / Middle 7–15 / Death 16–20), with `for_over()` lookup and `balanced()` + `textbook()` factories (phase boundaries as constants — `POWERPLAY_OVERS=6` per T20 law, `DEATH_START_OVER=16` tunable). Threaded as an optional trailing param through `simulate_innings()` (null → all-BALANCED, byte-identical to rung 3) and `simulate_match()` (routed to the **Player's batting innings only**; opposition stays Balanced). This is the sim's **first real decision input** — the lever the ADR 0003 naive-vs-optimal skill-gap metric measures; the Theme-6 swipe-card later writes into this same seam. **Rung 4 was split into 4a (Intent, done) + 4b (Bowling Change, next).** Built **inline test-first** (not subagent-driven — 3 tiny pure-domain tasks with zero test-isolation surface, the bug class subagent review exists to catch). 11 new tests (144, was 133): IntentPlan phase mapping + factories; innings null==balanced regression, determinism-with-plan, directional coupling (aggressive both outscores **and** out-dies defensive over 40 innings); match routing both directions (proven via the first-innings-uses-initial-RNG isolation trick) + determinism. Spec `docs/superpowers/specs/2026-06-07-intent-plan-design.md`, plan `docs/superpowers/plans/2026-06-07-intent-plan.md`. **Known:** the pre-existing HoF scene-test orphans are unchanged (rung 4a added only pure domain logic).
**Prior slice:** **Theme 7a rung 3 — full-match sim** (2026-06-07, PR #6). `MatchResolver.simulate_match()` calls `simulate_innings()` twice (Player's team + opposition), runs a chase, and decides a cricket-standard result — the **thinnest-wrapper** design (constant bowling, raw-int inputs, caller-supplied toss bool). **Settled the bowler-rotation open question: deferred** — constant bowling both sides this rung; real per-over rotation lands with the Bowling Change Key Moment (next rung). Two backward-compatible extensions to `simulate_innings()`: **optional statted Player** (`null` → all 11 derived, for the opposition innings) + **optional chase `target`** (third stop condition: ends the instant `total >= target`). `_decide_result()` extracted as a pure function so all three outcomes (incl. the hard-to-seed tie) are unit-tested on constructed innings. New `MatchResult` holder (both innings, `Outcome` enum, margin fields, `player_won()`/`is_tie()`/`margin_text()`). Files: `scripts/data/match_result.gd`, `scripts/domain/match_resolver.gd`, extended `scripts/domain/innings_resolver.gd`. Full brainstorm→spec→plan→build→merge cycle; **subagent-driven** (implementer → spec review → quality review per task, + holistic final review), test-first (18 new GUT tests across 4 tasks: determinism, optional-Player, backward-compat, chase-stops-early, win-by-runs/wickets/tie, perspective mapping, even-contest balance). Spec `docs/superpowers/specs/2026-06-07-full-match-design.md`, plan `docs/superpowers/plans/2026-06-07-full-match-plan.md`. **133 tests green** (was 115 pre-rung). **Known:** the 15 pre-existing HoF scene-test orphans on `main` are unchanged (rung 3 added only pure domain logic — structurally can't add scene orphans).
**Prior slice:** **Theme 7a rung 2 — single-innings batting sim** (2026-06-07, PR #5): `InningsResolver.simulate_innings()` loops the ball atom into one deterministic T20 innings — 11 batters, **build-driven batting position** (settles participation), weakening tail, strike rotation, 10-wkt/120-ball termination. `InningsResult` + `InningsTuning` strawman. Sandbox `docs/mockups/innings-sim-v1.html`. **Prior:** rung 1 — `resolve_ball()` (PR #3), two-stage logistic, `BallTuning`.
**Next theme:** **7c balance harness is underway**, decomposed A→E (Nico chose balance-first, 2026-06-08): **A platform ✅ (7c-1)** → **B skills ✅** (PR #16) → **C jokers-in-sim ⏳** — slice ✅ (PR #18) + **C2a field-mode ✅ (PR #19, 11 of 45 jokers)**; **C2 decomposed C2a→C2g** (one new sim subsystem + its jokers per rung): C2a field ✅ → **next = C2b bowling-side intent** → C2c Form → C2d windowed-trigger buffs → C2e `setNextBowler`-fire → C2f Manager Boost → C2g DRS/`tryReview` → **D ₸ economy** (skills-vs-jokers spend ROI) → **E self-play decision AI** (ADR 0003 optimal-vs-naive skill-gap; throttle AI competence per Level). Each later layer plugs into the 7c-1 `Sweep`/`Distribution`/viewer. The **mid-tier-flatness** watch-list item (see Notes) is a 7c sweep target. (Career multi-Season loop + optional 4c husbanding still deferred — built on `SeasonResolver` when balance is locked.)

---

## Next session

**⚖️ SCENARIO SWEEP (rung 3) + CAPPED-JOKER MECHANIC CHANGES both done (2026-06-10).** Conditional jokers are measured in firing contexts (chase profile + designed combos) with fire-rates for pricing; the two magnitude-capped jokers (Chase Master, Wicket Maiden) were then fixed with a **converting second component** (composure / economy) and both now reach their band floor. **366 tests green.** **Next per the 7c plan: D = the ₸ economy** (skills-vs-jokers spend ROI) — now fully unblocked.

**To continue (next Claude Code session):**

> Run the start-of-session routine. **State:** if the mechanic-change PR is merged you're on `main`; else on branch `capped-joker-mechanic-changes` (rung 3 = PR #34, merged). **366 tests green.** Conditional jokers measured + fixed; fire-rates captured. **Next rung = 7c-D, the ₸ economy** (price jokers + skills; ROI of spend) — confirm with Nico, then brainstorm→spec→plan→build as usual.
>
> **What this hands to D (the pricing inputs):** honest per-joker strength now exists for the conditional tail. **Pricing rule of thumb:** price on *realized* strength = in-condition × fire-rate (e.g. Chase Master realized ~+6.6% as a ~50%-fire Legendary). Numbers + fire-rates in spec `2026-06-10-scenario-sweep-conditional-jokers-design.md` §10 + `2026-06-10-capped-joker-mechanic-changes-design.md` §6 + the pool-doc notes.
>
> **The one design thread still open for Nico:** the **loadout cap** — full-archetype stack arms read high (Reviewer +34%, Boost +22.6%) but require holding *every* joker of a type (8/7); a ~5-slot loadout limit (a D/shop rule) keeps them out of real play. Not a per-joker magnitude bug — it's a shop/economy rule to settle in D. *(The "mechanic-capped jokers" thread from rung 3 is now **resolved** — both got converting second components.)*
>
> **The multi-row joker pattern (used twice this session):** to make a saturated/rare-trigger joker convert, add a **second `JokerEffect.make(...)` row with the same `id` and gate** but a complementary target (Chase Master: a `WICKET ×<1` survival row beside the `RUNS ×>1`; Wicket Maiden: a `RUNS ×<1` economy row beside the `WICKET ×>1`). The engine products multi-row jokers automatically (`JokerCatalog.implemented_groups()`), and `implemented().size()` counts flat rows — bump the count assert when adding rows.
>
> **The joker seams:** magnitudes in `scripts/data/joker_catalog.gd` (`mult` / `drs_p_bonus`); DRS dials in `scripts/data/drs_policy.gd` + `scripts/domain/joker_runtime.gd`. **Oracle = `tools/sweep_jokers.gd`** — now runs **two profiles**: `"standard"` (toss) + `"chase"` (forces bat-2nd via `MatchResolver.simulate_match_teams(..., force_player_bats_first=0)`); prints `CHASE_FIRE_RATE` then JSON with `"arms"` + `"chase_arms"`; combo/marginal arms (`Form-consumers only`, `Vigil + sources`) included. Catalog magnitude asserts in `tests/unit/test_joker_catalog.gd`; the toss seam tested in `tests/unit/test_match_resolver.gd`. **`build_spectrum_sweep.gd` is the *build* oracle; `sweep_jokers.gd` the *joker* oracle.**
>
> **Conventions** (project `CLAUDE.md`): GDScript tabs; `--import` once after adding scripts; GUT `-gtest` does **not** filter — judge red by parse-error/failing-assert, green by count climbing past **366**; **quit the Godot editor before headless runs** (`pgrep -x Godot` first); commit `*.gd.uid` for `scripts/`+`tools/`, not `tests/`; clean iCloud `" 2"` conflict files before a run. The two-profile sweep takes ~60–80s — run it backgrounded. **Concise replies — lead with a plain-English takeaway and point Nico at the one thing that matters.**

**Awaiting Nico — confirm the next rung:** the plan's **7c-D ₸ economy** is now unblocked (rung 3 produced the pricing inputs). Recommendation: **proceed to D**, deciding thread (a) above (price-cheap vs mechanic-change for the two capped jokers) as part of it. Alternatively, take on the **Chase Master / Wicket Maiden mechanic redesign** as a small rung *before* D if you want every Legendary to actually hit realized-band. Nico's call.

**Open auto-sim questions:** participation model — **settled at rung 2** (build-driven batting position). Bowler-rotation policy — **settled at rung 4b** (per-phase pace/spin via `BowlingPlan` + tilted `BowlingAttack` profiles). Batting Intent — **settled at rung 4a** (`IntentPlan` per phase). stars→numbers pipeline + Team interface + toss — **settled at 7b-1** (`TourDistribution` + `Team` + `_resolve_toss` + `simulate_match_teams`). Player-as-bowler — **settled at PR #16** (build-driven 0–4 over quota via `InningsResolver.player_overs`; the Player's Attack/Control now feed the opposition innings). Still open / deferred: **toss bat/bowl heuristic** (winner always bats first for now), **distinct attack vs control per Team** (one bowling number for now; pace/spin tilt differentiates inside the innings), **bowler husbanding** (pool/cap/decay → optional 4c), **Player bowling stat line** — **built at PR #17** (D5: `InningsResult.player_bowl_wickets/runs/balls`); **Player bowling type (pace/spin)** still deferred (D4), **opponent batting + bowling intelligence** (opposition bats Balanced, bowls textbook → later rung).

**Loose thread:** 15 orphaned nodes in the GUT suite (pre-existing, from the merged Hall of Fame scene tests) — flagged as a background task (`task_13d395f2`), not introduced by Theme 7a.

---

## The themes (ordered by dependency)

1. ~~**Scope**~~ ✅ — V1 locked: SA+AUS roguelite cricket-career (ADR 0002)
2. ~~**Game shape**~~ ✅ — Career grid (Levels × Tours), Offers/Tons/Affinity (CONTEXT.md)
3. ~~**Gameplay & balance**~~ ✅ — sim math (ADR 0004), skill model (ADR 0003), Manager Boost (ADR 0005), 6-verb effect palette (ADR 0006), all 8 KMs designed. Numerical tuning deferred to the balance harness.
4. ~~**Jokers & meta loop**~~ ✅ — Architecture (Shop cadence, authoring grammar ADR 0007, carry-over, Team durability ADR 0009) + Content (all 45 Jokers authored at `docs/joker-pool-v1.md` — 24 Common / 15 Rare / 6 Legendary across 6 archetypes, magnitudes V1 strawman for the balance harness).
5. ~~**Around-the-match screens**~~ ✅ — Season hub · Pre-match · Shop · Offer · Result · Career Grid · Career Records (ADR 0010 navigation shell · ADR 0011 global rank · hi-fi at `docs/mockups/around-the-match-v1.html` · DESIGN_HANDOFF §17). **Onboarding now closed** — Player Creation spec at `docs/superpowers/specs/2026-06-01-player-creation-design.md` covers the 2-screen Identity → Build flow + hard-permadeath lifecycle. **Hall of Fame screen spec now delivered** (`docs/superpowers/specs/2026-06-02-hall-of-fame-design.md`) — the last Theme 5 sibling-spec gap; it builds later as the real screen replacing the Player Creation plan's HoF stub. Settings still deferred.
6. **Visual & audio content** — joker art · backgrounds · icons · music · SFX · commentary bank · **8 portrait sets** (4 Appearance × 2 Country) + Player Creation backgrounds (per Player Creation spec)
7. **Tech foundation** ⏳ *in progress* — Godot project ✅ · save system ✅ · build pipeline · the **match sim** (ADR 0004: ✅ `resolve_ball()` · innings · match · Intent (4a) · bowling (4b) · Team/stars/toss (7b-1) · league (7b-2a) · playoffs+outcome+★mutation (7b-2b) — **Season loop complete**) · ⏳ **balance harness (7c)**: ✅ **A platform (7c-1)** `Sweep`+`Distribution`+viewer · ✅ **B skills** (Player-as-bowler wired, PR #16) · ✅ **C jokers-in-sim COMPLETE** (PRs #18–#26: slice + C2a–C2h → all 45 jokers wired & swept; `JokerEffect` tuple + stateful `JokerRuntime` engine) · ✅ **balance-tune** (build-balance LOCKED + fair-fight baseline + joker re-tune to rarity bands, 2026-06-10) · ⏳ D ₸ economy (next — but see the scenario-sweep prerequisite in Next session) · E self-play AI · (Career multi-Season loop = a Theme-2 gameplay rung built on `SeasonResolver`, after balance) (headless sim runner — parameter sweeps over Tour distributions, KM/Joker/Boost coefficients, and the optimal-vs-naive skill-gap metric per ADR 0003/0004)
8. **Long-haul retention** — Collection / Achievements layer (77-feat trophy cabinet across 6 categories with Tons rewards) · streaks · daily prompts · any other replay-extending systems. Deferred until after the build is playable.
9. **Career pressure & retirement** (post-launch) — auto-drop / non-selection / age-based retirement triggers. Layered on top of the hard-permadeath lifecycle established in the Player Creation spec; trigger anchors on **Player form** (low runs over last K innings, lost KMs), not Team result. Deferred until V1 ships and the Career-as-life baseline loop is playtested — the cheap escape-hatch (Manual retire button) covers the *"I'm stuck"* case in the meantime.

Playtest threads through all of them — continuous, not a discrete item.

---

## Open design questions

Surfaced 2026-06-02 while pressure-testing the Player Creation plan. Both are **auto-sim design decisions (Theme 7)** — non-blocking for the Player Creation build, but must be settled before/during the sim build. Both sit downstream of ADR 0004 (only-Player-statted; the 21 other players are derived from `teamStrength × role × seed`, not individually authored).

1. **Player participation model** — how much the Player personally bats (batting position → balls faced) and bowls (overs) each Match. Candidates: *fixed participation* (build changes only how **well**, not how **much**; bowler-shaped Players still bat, and bat badly emergently) vs *attribute-driven participation* (a bowler-shaped build bats lower / bowls more automatically). Either keeps the classifier label flavour-only per spec §3.5. **Simplest-first leaning: fixed.**
2. **Bowler-rotation policy** — how an innings' ~20 bowling overs get assigned across bowler-role profiles. *Player side* surfaces as agency via the **Bowling Change** Key Moment; *opponent side* needs an AI rotation policy (currently un-spec'd). Note the scope wall: opponents are regenerated per-Match with no persistent identity or cross-Match form, so "their star bowler is out of form" is **texture-via-commentary**, not simulation — the cheap third door that avoids rebuilding the deferred management sim.

---

## Decisions log

| Date | Decision | Rationale |
|---|---|---|
| 2026-06-08 | **⚖️ Build-balance opened — D1–D5 (spec `2026-06-08-build-balance-rating-and-team-design.md`)** | Goal: all-rounder *as good as* batter/bowler, balanced at **team** (every build ~50% win at even ★3 — build is win-neutral; power comes from jokers/skills) **and individual** (equally valuable in one currency). **D1** target = flat ~50% (not "specialists earn an edge"). **D2** rating currency = **net runs** (runs added + runs saved, wickets as run-value). **D3** team model = **discrete real roster** — every player a 20-pt build (sum 20, [1,8]); a team = 220 pts; opponent = fixed standard XI (6 BATTER·1 ALLR·4 BOWLER → 122/98 split) for now. **D4** the balancing is a real game system (lives in `scripts/`), not a harness trick. **D5** balance sweep stays neutral-Intent. Slices: 1 rating ✅ → 1b wicket-fix ✅ → 2 discrete roster → 3 gap-fill + flatten + re-solve `wicket_value`. |
| 2026-06-08 | **🎉 Layer C COMPLETE — C2g + C2h merged (PRs #25, #26): all 45 jokers wired** | **C2g (Form sources & intent dynamics, PR #25, 39→43):** the Form-*source* jokers that generate Form events + a Form→intent feedback. `JokerEffect.FormSource` (ON_BALANCED/ON_AGGRESSIVE/ON_DEFENSIVE_OVER) + `snaps_intent`; `JokerRuntime.fire_form_source` fires the C2c Form channel; `simulate_innings` detects intent switches (#2/#11), counts consecutive Player Defensive balls (#5), latches intent to Aggressive on a Form event (#13). Sources are enablers (+0.0% solo — value via Form consumers); Boundary Hunter +3.4% (intent-snap); Form-source combo +6.1%. **C2h (the final two, PR #26, 43→45):** #9 Field Restrictions (reuses `field_req` by routing the **opposition's field** into the Player's batting innings via new `opp_field_plan`) + #15 The Chase Master (new `chase_req`, `is_chase = target>0`; the "Form extends the window" framing collapses to an always-on per-ball buff). #9 +3.9%, #15 +1.9%. **Capstone full-pool sweep:** archetype-stack win-deltas — **Reviewer +31.4% (top tuning priority — review-to-survive → near-immortal)** › Boost +18.1% › Batting +13.0% › Form-window +8.5% › Wicket-Hunter +6.6% › Form-source +6.1% › Bowling-intent +5.8% › Field-defensive +5.1%; **all 45 at once wins 100%**. 15 new tests across the two (330, was 315). Specs/plans `…-7cC2g-form-sources-…` / `…-7cC2h-final-…`. **Layer C done; next = balance-tune (Nico's call) or layer D (₸ economy).** |
| 2026-06-08 | **C2f (DRS / tryReview) built + merged** (PR #24) — 39 of 45 jokers | The Reviewer archetype + the last new mechanic. New `DRSPolicy` (base_reviews/base_p); a review is attempted at a close decision (batting: Player dismissed → survive; bowling: Player-bowled dot → claim) by mutating the `BallOutcome` so existing handling takes over. `JokerEffect.DRSRole` (ACCURACY/EXTRA_REVIEW/RETAIN/FORM_ON_SUCCESS/BOWLING_BUFF/MASTER) + `drs_p_bonus`; runtime-owned. `JokerRuntime.init_reviews` + `try_review` (one randf/attempt, retain-on-success, consume-on-fail unless retain joker, success payoffs). Wires **#38–#45**. RNG consumed only on an actual attempt → no-policy byte-identical; determinism preserved. **⚠️ Major finding:** **Reviewer stack +31.4% (mean runs 30.9) — the pool's most overpowered combo** (the Player reviews every dismissal → near-immortal); Review Master +14.9% solo, Snicko +7.3%, Captain's Call +4.7%; Captain's Eye/Hot Spot/Bowler's Backing weak. Inline test-first (6 tasks); 14 new tests (315, was 301) incl. new `test_drs_policy.gd`. Spec `…-7cC2f-drs-design.md`, plan `…-7cC2f-drs.md`. **Next = C2g (Form sources & intent dynamics: #2,#5,#11,#13).** |
| 2026-06-08 | **C2e (Manager Boost / Boost Stack) built + merged** (PR #23) — 31 of 45 jokers | The trickiest archetype: the jokers *modify a base mechanic* (the Boost press) rather than firing independent buffs. Models the Manager Boost (ADR 0005) as a player-timed press (`BoostPlan` = press_overs + base_mult/base_n) firing a side-aware base buff (runs batting / wicket bowling) via `JokerRuntime.on_boost_press`. New `JokerEffect.BoostRole` enum; boost_role jokers owned by the runtime (`matches()` false). `on_boost_press` applies modifiers (Power Up n+2 / Power Surge mult×1.2 n+3 / Comeback 3rd-press mult×1.5 n+6), pushes base + chain buffs (Battery kicker, Compounding when Aggressive, Pedal flips to Aggressive), and fires the C2c Form channel for Adrenaline/Comeback (Boost→Form synergy, reusing `_fire_form_event`). Wires **#31–#37**. Threaded off-by-default through `simulate_innings` + **both** innings of `simulate_match(_teams)`; determinism preserved. **Decisions (spec):** D1 base Boost is a baseline mechanic (in every sweep arm); D2 side-aware roll-mult buff (ADR's fill² shaping deferred); D3 Pedal/Adrenaline are enablers (≈0% solo); D4 #33/#37 fire the Form channel; D5 press in both innings for coverage (single-Boost economy deferred). **Data** (3 presses overs 1/10/16): Power Surge +3.1% (strongest modifier), Compounding +2.9%, Comeback +1.5%, Battery +1.1%, Power Up +0.8%, Adrenaline/Pedal +0.0%; **Boost-stack stack +16.2% — pool's most powerful combo** (the pool doc's flagged highest-variance state). Inline test-first (6 tasks); 15 new tests (301, was 286) incl. new `test_boost_plan.gd`. Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC2e-boost-design.md`, plan `…-7cC2e-boost.md`. **Next = C2f (DRS).** |
| 2026-06-08 | **C2d (setNextBowler-fire) built + merged** (PR #22) — 24 of 45 jokers | Reuses the C2c `JokerRuntime` engine via a **second event channel**: a bowling change fires buff windows. `setNextBowler` event = a spell start at overs 1/7/16 in the opposition's batting innings (Player captains the bowling), carrying the `BowlingPlan.Kind`. `JokerEffect.Trigger` += CHANGE_PACE/SPIN/ANY (CHANGE_ANY reuses `field_req` as a gate); new stateless `bowler_type_req` for #28. `JokerRuntime.on_bowling_change` pushes a buff from the change over. Wires **#24 Pace Pack**, **#25 Spinner's Web**, **#27 First-Change Specialist**, **#30 The Strike Bowler** (catching-change), **#28 The Trap** (catching+spin, stateless). #27 field-set + #30 Form-grant simplified away (EV buff kept). Off-by-default (no BowlingPlan → no events), determinism preserved (fixed overs, no RNG). **Data:** The Strike Bowler +1.7% (standout), First-Change +0.6%, The Trap +0.4%, Pace Pack +0.3%, Spinner's Web +0.1%, Wicket-Hunter stack +4.5% — mild setNextBowler magnitudes = a balance-tune candidate; the sweep now enables rotation (baseline ~40%). Inline test-first (5 tasks); 11 new tests (286, was 275). Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC2d-setnextbowler-design.md`, plan `docs/superpowers/plans/2026-06-08-jokers-in-sim-7cC2d-setnextbowler.md`. **Next = C2e (Manager Boost).** |
| 2026-06-08 | **C2c (Form & windowed-trigger buffs) built + merged** (PR #21) — 19 of 45 jokers | First **stateful** joker layer. New `JokerRuntime` (`scripts/domain/joker_runtime.gd`) holds per-innings decaying buffs that a **Form event** fires (Player boundary while batting / Player wicket while bowling — uses only existing sim signals); `JokerResolver` stays pure. `JokerEffect` gains a `Trigger` enum (NONE/FORM_BAT/FORM_BOWL/FORM_DOUBLE_BAT) + `window_n`; trigger jokers are invisible to `matches()`. Wires **#10 Ride the Wave** (runs ×1.20 n=3), **#29 Wicket Maiden** (wicket ×1.30 n=6), **#14 Hot Streak** (2 events in 6 → runs ×1.30 & wicket ×0.85 n=6), **#7 Match-Winner's Vigil** (wicket ×0.80 n=24). **Decisions (spec):** D1 merged the planned C2c "Form" + C2d "windowed buffs" into one rung (inseparable) → later rungs shift down; D2 Form event = boundary/wicket; D3 buffs apply to every ball in the window on the matching side (consistent with existing per-innings stateless jokers); D4 `JokerRuntime` is the stateful layer, resolver stays pure; D5 #7's "snap to Defensive" intent-write simplified away (buff is the EV part); D6 deferred the Form-source/intent-snap stragglers (#2,#5,#11,#13,#15) to a cleanup rung. Threaded internally through `simulate_innings` (no new public params); determinism preserved (no new RNG draws), empty/stateless-only lists byte-identical. **Data** (`tools/sweep_jokers.gd`): Match-Winner's Vigil +3.9% (strongest — 24-ball survival window), Hot Streak +1.6%, Ride the Wave +1.0%, Wicket Maiden +0.2% (weak — balanced build bowls few overs to trigger), Form-window stack +5.2%. Inline test-first (6 tasks); 17 new tests (275, was 258) incl. new `test_joker_runtime.gd`. Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC2c-form-windowed-design.md`, plan `docs/superpowers/plans/2026-06-08-jokers-in-sim-7cC2c-form-windowed.md`. **Next = C2d (setNextBowler-fire).** |
| 2026-06-08 | **C2b (bowling-side intent) built + merged** (PR #20) — 15 of 45 jokers | Second C2 sub-rung. Adds the **bowling-captain intent** signal (`bowl_intent`, reusing `IntentPlan`) routed to the opposition's batting innings (mirror of `FieldPlan`), plus an **opposition AI batting intent** so a "reads the batsman" joker can fire. Wires **#26 Attack the Stumps** (bowl_intent Aggressive → wicket ×1.12), **#19 Pressure Cooker** (opposing batsman Defensive → wicket ×1.10), **#22 Choke Hold** (Legendary: defensive field + Defensive captaincy → runs ×0.75 & wicket ×1.15), **#18 Defensive Captain** (Defensive captaincy *sets* a defensive field — an enabler via a `sets_field` pre-pass in `JokerResolver`, no roll of its own). **Decisions (spec):** D1 reuse `IntentPlan` for both new intents (no new class); D3 two new `JokerEffect` fields `bowl_intent_req` + `sets_field` (both `int`, default -1; `matches()` gains trailing `bowl_intent`); D4 #18 as a field-deriving enabler (reads ≈0% solo like Rotate the Strike — value is combinatorial, proven by a #18+Tight Lines unit test); D5 threaded off-by-default through `simulate_innings` (`bowl_intent_plan`) + `simulate_match(_teams)` (`player_bowl_intent_plan`, `opp_intent_plan`) → byte-identical for existing callers, determinism preserved (intent shifts the logit, no new RNG draws); D6 opp-intent feeds both the resolve_ball logit and #19's gate — sweep holds both plans constant across arms to isolate the joker. **Data** (`tools/sweep_jokers.gd`, 2000 even ★3, shared bowl/opp intent + field plans): Attack the Stumps +1.4%, Choke Hold +5.0% (strongest single), Pressure Cooker +0.1% (fires only in the opp's Defensive middle overs), Defensive Captain +0.0% (enabler), Bowling-intent stack +7.0%. Inline test-first (6 tasks); 18 new tests (258, was 240). Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC2b-bowling-intent-design.md`, plan `docs/superpowers/plans/2026-06-08-jokers-in-sim-7cC2b-bowling-intent.md`. **Next = C2c (Form).** |
| 2026-06-08 | C2 decomposed C2a→C2g; **C2a (field mode) built + merged** (PR #19) | Auditing the 45-joker pool against the sim showed **C2 is not one rung** — the 40 remaining jokers need **six distinct new sim subsystems** (field mode, bowling-side intent, Form, windowed-trigger buffs, setNextBowler-fire events, Manager Boost, DRS). So **C2 = C2a→C2g**, one subsystem + its jokers per rung (mirrors 4a/4b, 7b-1/7b-2). **C2a = field mode**, taking the implemented pool **5 → 11 of 45**. **Decisions (spec):** D1 the split itself; **D2 field mode is a readable captain *condition* jokers gate on, no intrinsic roll EV this rung** (layer C wires joker effects, not new gameplay levers — "field as a real lever" is a future rung; keeps scope tight + determinism trivial); D3 joker set #4/#6/#12 (stateless leftovers) + #16/#20/#23 (field) spans multi-buff + combined intent+window + catching/defensive × wicket/runs; D4 **multi-buff via duplicate-`id` rows** (no `JokerEffect` model change; `JokerCatalog.implemented_groups()` groups them); D5 `FieldPlan` per-phase (NEUTRAL default, mirror of `IntentPlan`/`BowlingPlan`), routed to the opposition's batting innings (Player sets the field while bowling), no new RNG draws. **The data** (`tools/sweep_jokers.gd`, 2000 even ★3/arm, shared intent + field plan): Dot Ball Pressure +4.6%, Tight Lines +3.6%, Cordon Killer +1.3%, Slog Over Specialist +4.6%, field-defensive stack +6.6%, batting stack +10.2%; original 5 reproduce PR #18 (regression sanity); Rotate the Strike +0.0% (sweep plan never sits Balanced — covered by unit test). Inline test-first (5 tasks); 21 new tests (240, was 219). Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC2a-field-design.md`, plan `docs/superpowers/plans/2026-06-08-jokers-in-sim-7cC2a-field.md`. **Next = C2b (bowling-side intent).** |
| 2026-06-08 | Jokers-in-sim **vertical slice** built + merged — 7c harness layer C started (PR #18) | Nico's call: **vertical slice first** (build the effect-application seam + 5 representative jokers end-to-end, prove the pattern) rather than all 45 in one rung. **The insight:** almost every joker bends one of `resolve_ball`'s two rolls (`p_wicket` / scoring-strength `s`), so the seam = let an active-joker layer multiply them per ball. `JokerEffect` data tuple + pure `JokerResolver.roll_mults() → Vector2(wicket_mult, runs_mult)`, threaded as `resolve_ball(..., wicket_mult, runs_mult)` (clamped, **RNG-count-preserving → determinism kept**) and optional `jokers`/`player_is_batting` through `simulate_innings`/`simulate_match(_teams)`. **Decisions (spec):** D2 slice only the **stateless "while CONDITION: buffNextBalls(wicket\|runs) per ball"** sub-grammar (defer windowed-trigger + Form/DRS/field/Boost/setNextBowler-fire to 7c-C2); D3 the 5 jokers (Dead Bat, Powerplay Punch, Block the Shine, Squeeze the Middle, Death-Over Stranglehold) span both targets/directions/sides + intent & ball-window conditions + Common/Rare, **using only state the sim already tracks**; D4 seam in `resolve_ball`, mirrors `IntentPlan`/`BowlingPlan` threading; D5 "runs ×m" scales `s`, "wicket ×m" scales `p_wicket`, both clamped; D6 bowling-side slice jokers use ball-window conditions only (no bowling-captain intent modelled yet). Also: `JokerEffect` fields stored as `int` not enum-typed (GDScript can't unify a class-local enum type across the call boundary). **The data** (`tools/sweep_jokers.gd`, 2000 even ★3/arm, shared intent plan): Powerplay Punch **+5.5%** win-rate (strongest Common), Squeeze/Death-Over ~+3%, Dead Bat/Block-the-Shine <+1%, batting stack **+7.4%** (≈ sum of parts). Inline test-first (7 tasks); 22 new tests (219, was 197). Spec `docs/superpowers/specs/2026-06-08-jokers-in-sim-7cC-slice-design.md`, plan `docs/superpowers/plans/2026-06-08-jokers-in-sim-7cC-slice.md`. **Next 7c step = C2 (scale to all 45 + deferred verbs).** |
| 2026-06-08 | Player-as-bowler built + merged — 7c harness layer B (skills) complete | Nico's call: close the 7c-1 "bowling build is inert" finding before jokers. The Player's bowling Attributes (Attack/Control) now feed the sim, mirroring build-driven batting position: `InningsResolver.player_overs(attrs, itun)` maps the batting/bowling **share** → a **0–4** over quota (strawman curve in `InningsTuning`; batter 0 / balanced 2 / specialist 4), `player_bowling_overs(n, total)` spaces the overs; threaded as 3 off-by-default params through `simulate_innings` (Player over → batter faces the Player's Attack/Control, overriding team scalar / pace-spin plan) and computed-from-the-real-Player in `simulate_match` (opposition innings; no new RNG draws → determinism kept). **Result:** bowling-build win-rate **48%→62%** at even team strength (wins through bowling, still avg ~3 runs batting); batting (67%) ≈ bowling (62%) → no dominant build (ADR 0003). **Decisions (spec):** always-on not opt-in (D1, **deliberately ends the byte-identical-core invariant** held since rung 3 — opposition outcomes shift for non-batting Players; one snapshot baseline updated, all directional/determinism tests stand); build-driven 0–4 overs reusing the share signal (D2); evenly-spaced overs (D3); Player bowls raw Attack/Control, no pace/spin tilt (D4 deferred); no Player bowling stat line yet (D5 deferred). Inline test-first (4 tasks); 10 new tests (194, was 184). Refreshed `docs/mockups/distribution-viewer-v1.html` (win-rate column tells the new story). Spec `docs/superpowers/specs/2026-06-08-player-as-bowler-design.md`, plan `docs/superpowers/plans/2026-06-08-player-as-bowler.md`. PR #16. **Next 7c layer = C jokers-in-sim (recommended; Nico most wants it).** |
| 2026-06-08 | Theme 7c started balance-first + decomposed A→E; rung 7c-1 (harness platform) built + merged | Nico chose **balance harness before the Career rung**, with a big vision (run 1000s of scenarios; see how **skills vs jokers** spending shifts the distribution; model the **₸ economy**; an **AI that self-plays to find optimal strategies**, throttled per Level). Flagged it as a multi-rung theme and **decomposed A→E**: **A platform** → **B skills** (needs Player-as-bowler — bowling attrs currently inert) → **C jokers-in-sim** (build the 45 Jokers' 6-verb effects, then sweep) → **D ₸ economy** (skills-vs-jokers ROI) → **E self-play decision AI** (optimal-vs-naive skill-gap ADR 0003 + per-Level difficulty throttle). Built **A (7c-1, PR #14)**: `scripts/harness/` lib — `Distribution` (stats+histogram) + `Sweep` (model-agnostic `run(arms,n,scenario,base_seed)`, paired seeds across arms) — and a **reusable** overlaid-histogram viewer `docs/mockups/distribution-viewer-v1.html`. Demo `tools/sweep_skills.gd`: build sweep over even ★3 matches → batting 43.7 runs/67% win, balanced 13.1/51%, bowling 2.7/48%. Surfaced the **Player-as-bowler inert-lever gap** as data. Inline test-first (4 tasks); 7 new tests (184). Spec `docs/superpowers/specs/2026-06-08-balance-harness-platform-7c1-design.md`, plan `docs/superpowers/plans/2026-06-08-balance-harness-platform-7c1.md`. **Next 7c layer = C jokers-in-sim (or a Player-as-bowler rung to complete B), Nico's call.** |
| 2026-06-08 | Theme 7b rung 7b-2b — Season playoffs + outcome + ★ mutation built + merged | Completes the Season loop (PR #13). `SeasonResolver.simulate_season()` = league (7b-2a) → top-4 knockouts (semis 1v4/2v3 → The Final + 3rd-place playoff, reusing the per-Season held strengths now exposed on `LeagueResult.team_bat/bowl`; Player statted via the `_knockout` slot helper; ties → higher seed advances) → `SeasonResult` (`final_order`, `player_final_position`, `beat`=top3, `won_final`=1st). Added `Team.mutate_stars(rng)` (ADR 0009 rollover: ±0.5 ~30% / ±1.0 ~5% / 65% none, clamped — a primitive, not yet looped). Decisions: one rung for playoffs+outcome+mutation (D1); reuse held strengths (D2); tie→higher seed (D3); `won_final` is raw championship, "win the Level" Premium gating + multi-Season Career loop deferred to the Career rung (D5). Eyeball `tools/season_outcome_preview.gd` → `docs/mockups/season-outcome-v1.html` (beat + win-Final vs ★; even a 5★ wins the Final only ~55% — two knockout coin-flips). Inline test-first (4 tasks); 8 new tests (177, was 169). Spec `docs/superpowers/specs/2026-06-08-season-playoffs-7b2b-design.md`, plan `docs/superpowers/plans/2026-06-08-season-playoffs-7b2b.md`. **Season loop now complete; next = Career rung or 7c harness (Nico's call).** |
| 2026-06-07 | Theme 7b rung 7b-2a — Season league phase built + merged | Split 7b-2 into **7b-2a** (league phase, this) + **7b-2b** (playoffs + beat/win + ★ mutation), same pattern as 4a/4b + 7b-1/7b-2. Built 7b-2a (PR #12): `LeagueResolver.simulate_league()` plays an 8-team single round-robin (28 games, each plays 7), every game simulated — Player games statted, non-Player games via the already-supported `simulate_match(null,…)` derived path (one code path, real table). Each Team's strength drawn **once per Season** (held all Season, ADR 0009 noise hoist). `StandingsRow` (points + runs/balls for/against + `nrr()`), ranked **points → NRR → index**, into `LeagueResult` (standings, player_position 1–8, made_playoffs = top 4, player_matches). Decisions recorded in spec: full-sim all games (D2), per-Season draw (D3), per-match toss (D4), points 2/1/0 + NRR tiebreak with actual-balls simplification (D5). Eyeball `tools/league_preview.gd` → `docs/mockups/league-finish-v1.html` (qualification + avg finish vs ★ over random fields; both climb with ★, qual crosses 50% at ~★3.5). Inline test-first (4 tasks); 6 new tests (169, was 163). Spec `docs/superpowers/specs/2026-06-07-season-league-7b2a-design.md`, plan `docs/superpowers/plans/2026-06-07-season-league-7b2a.md`. **Next = 7b-2b.** |
| 2026-06-07 | First harness-driven balance tune — `TourDistribution.spread` 3.0 → 1.5 (and made `float`) | Nico flagged the 7b-1 win-rate curve as too steep (a 2-tier underdog beating a 5★ only ~3%; he expected ~20%). Root cause: a match aggregates ~240 balls, so even a small per-ball edge compounds into a near-certain result — the curve is inherently steeper than the per-ball gap feels. The clean dial is `spread` (how many strength points a star is worth), flagged strawman for exactly this. Ran a sweep over spread ∈ {1.0…3.0} via a throwaway `tools/spread_sweep.gd`; **1.5** best fit the desired feel — 1-tier underdog ~33%, 2-tier ~17%, minnow-vs-giant ~0%, while stars still discriminate cleanly at the extremes (spread 1.0 let the ±1 noise drown the star signal — giant only won 95.5%). Changed `spread` from `int` to `float` so a star can map to fractional strength points. Regenerated `docs/mockups/star-winrate-v1.html` at 1.5. The first real balance decision made *through* the harness — exactly its purpose (ADR 0004). 163 tests still green (unit tests pass explicit tuning values; only the default changed). |
| 2026-06-07 | Theme 7b rung 7b-1 — Team · stars→numbers · toss · Team-bundled match built + merged | Split 7b into **7b-1** (play one Match between two ★-rated Teams) + **7b-2** (the Season league loop), mirroring 4a/4b. Built 7b-1 (PR #9): the sim gains the game's real vocabulary on top of the **byte-identical** `simulate_match()` core. `TourDistribution` (`mean ± spread`; `percentile(stars/5)→strength`, strawman 5/3/1) + `Team` (★ 0.5–5.0; `batting/bowling_strength = percentile(stars/5)+noise`, 2 independent draws floored at 1, ADR 0009) + seeded toss (`_resolve_toss`, replaces the `player_bats_first` bool). New entry point `MatchResolver.simulate_match_teams(player, playerTeam, oppTeam, tour, …)` derives 6 ints + flips the toss (fixed RNG order: toss→4 draws→core) and delegates to the unmodified core. **Eyeball deliverable** (Nico's "see the distribution" ask): `tools/season_preview.gd` runs the real sim across star matchups → `docs/mockups/star-winrate-v1.html` charts win-rate vs star gap — a clean S-curve (~54% at gap 0, 0%/100% at ±4.5); first seed of the 7c balance harness. Inline test-first (4 tasks); 9 new tests (163, was 154). Added `.claude/launch.json` for static mockup previews. Spec `docs/superpowers/specs/2026-06-07-season-wrapper-7b1-design.md`, plan `docs/superpowers/plans/2026-06-07-season-wrapper-7b1.md`. **Next = 7b-2 (Season league loop + ★ Markov mutation).** |
| 2026-05-18 | In-match screen design locked | See `docs/DESIGN_HANDOFF.md` Sections 3–4, 16 |
| 2026-05-18 | Avatar: Card Portrait + split-bg | Skill tier (sky) + confidence tier (ground) + 3-layer system |
| 2026-05-18 | Match length: 2:45 | India ARPU + session length fit (research-backed) |
| 2026-05-18 | Manager Boost: water-meter | Strength = fill %; tactical-depth via timing |
| 2026-05-18 | No IPL trade dress | Personal project · steer safe on IP |
| 2026-05-18 | Soft launch markets: PAK / BAN / SLK / UAE | Cricket audience · no India burn (when launch becomes relevant) |
| 2026-05-19 | Engine: Godot 4.x | Best fit for 2D card/UI with juice; AI handles ramp |
| 2026-05-19 | Launch track deferred | Build the game first; launch playbook saved for later |
| 2026-05-19 | Next-session skill: `grill-with-docs` | Better than `brainstorming` for conceptual work; opinionated + glossary discipline |
| 2026-05-21 | V1 = roguelite cricket-career | ADR 0002 — you are one rising Player climbing a Career grid |
| 2026-05-21 | Primary target: SA + AUS | ADR 0001 — creator-fit beats market-fit; India deferred |
| 2026-05-21 | Career grid: 3 Levels × 8 Tours | Club/City/Province × named tours; overlapping difficulty bands |
| 2026-05-21 | Career layer: Offers / Tons / Affinity | Light career sim; full club-management deferred |
| 2026-05-21 | `CONTEXT.md` created | Domain glossary — source of truth for terminology |
| 2026-05-22 | Skill model locked | ADR 0003 — 3 layers (Tactical / Strategic / Meta), no-dominant-answer, never surface the math |
| 2026-05-25 | Auto-sim architecture locked | ADR 0004 — headless, deterministic-seedable, ball-by-ball, 2-stage logistic, 4 Attributes (Power/Composure/Attack/Control), only-Player-statted |
| 2026-05-26 | 5 remaining Key Moments designed | Wicket Crisis, Milestone Ball, Bowling Change, Field Set, DRS Review |
| 2026-05-26 | Portrait redesign | Full-bg = skill, facial expression = Form (supersedes split-bg, DESIGN_HANDOFF §16.3) |
| 2026-05-27 | Manager Boost simplified | ADR 0005 — no menu, side-aware all-Attribute buff, drain-on-press, fill² scaling |
| 2026-05-28 | 6-verb typed-effect palette | ADR 0006 — every in-Match mechanic composes from `setIntent` / `setNextBowler` / `fieldMode` / `buffNextBalls` / `formEvent` / `tryReview` |
| 2026-05-28 | Theme 3 closed | Gameplay & balance design complete; numerical tuning is balance-harness work |
| 2026-05-28 | Currency named Tons (₸) | ADR 0008 — "Pay" working name dropped; ₸ glyph (T struck by a bail, two bails standard) locked. Assets in `docs/brand/`. |
| 2026-05-28 | Joker authoring grammar locked | ADR 0007 — passive verb buffs + conditional triggers, nothing else; synergies emerge from shared verb tags (no authored chain logic). |
| 2026-05-28 | Team strength durability via half-★ Markov rating | ADR 0009 — Teams carry durable 0.5–5★ rating in half-star increments, ~30% ±0.5 / ~5% ±1 per Season + `lastSeasonEvent` flavour string for ±1 swings; makes Offers a real Meta-layer signal. |
| 2026-05-28 | Meta-loop shape locked | Shop cadence (5 visits/Season Slay-style) · 4 actions per visit (buy/sell/upgrade/hold) · 45-Joker pool across 6 archetypes · Joker carry-over (1 owned, end-of-Season) · slot 4 earned at first Level-win · mid-Season Offer is immediate-switch. |
| 2026-05-29 | Team durability refined to half-★ + Theme 4 closed | ADR 0009 revised — stars on 0.5–5.0 in half-star increments (was integer 1–5); ±0.5 normal swings, ±1 catastrophic. Theme 4 (Jokers & meta loop) now closed; next theme is Around-the-match screens. |
| 2026-05-30 | Theme 5 architecture locked, hi-fi pass moved to claude.ai | Decisions: **Season hub** is the persistent home within a Season (`CONTEXT.md` updated); **Shop & Offer** are forced full-screen interludes triggered by cadence (no skip, no hub-overlay); **no Main Menu** (resume-first cold start, settings via corner gear); Season hub is **single dense screen** (no scroll, no tabs); Pre-match is **versus-style** (red-gradient `[A] vs [B]` header continues into Match chrome). Mockup pass spec'd in `docs/THEME-5-HANDOFF.md`; claude.ai produces hi-fi HTML at `docs/mockups/around-the-match-v1.html`. |
| 2026-05-31 | Theme 5 closed; ADRs 0010 + 0011 written; Theme 4 reopened (content); Theme 8 added | Around-the-match hi-fi accepted (`docs/mockups/around-the-match-v1.html` + `docs/design-handoff-from-claude-2026-05-31.md`). **ADR 0010** captures the navigation shell. **Career Records adopted** (global rank `#X / 2,112` computed analytically from Tour distributions — no per-rival sim) — **ADR 0011**. **Collection / Achievements deferred to new Theme 8** (Long-haul retention). **Theme 4 reopened** as architecture ✅ + content ⏳ — the 45 specific Jokers are unauthored and are now the next session. **Kit Room** / **Kit Manager** added to CONTEXT.md as canonical world-building. Australia palette fix: was a near-clone of SA green; now amber-gold primary + green accent (DESIGN_HANDOFF §16.2 updated). Onboarding + Settings screens deferred to a later pass within Theme 5's spirit. |
| 2026-06-01 | Theme 4 content closed + Theme 5 onboarding closed + Theme 9 added | All 45 Jokers authored (`docs/joker-pool-v1.md`) across 6 archetypes / 3 rarities, magnitudes V1 strawman for the harness. **Player Creation spec** (`docs/superpowers/specs/2026-06-01-player-creation-design.md`) closes Theme 5's deferred onboarding work: 2-screen Identity → Build flow · 5 decisions (Country / City / Appearance / Name / Attributes) · hard-permadeath lifecycle with Win + Manual retire end-states · live classifier label · 4 Appearance buckets per Country (no textual labels) · cricket-pun name gen (random + re-roll, no manual override). **ADR 0012** to formalise the lifecycle decision at plan time. **New Theme 9 — Career pressure & retirement** (post-launch) holds auto-drop / non-selection / age-based retirement; trigger anchors on Player form not Team result. |
| 2026-06-01 | Soft-launch markets corrected to SA + AUS | The 2026-05-18 "PAK / BAN / SLK / UAE — no India burn" decision was made under the India-primary-target era. With ADR 0001 making SA + AUS the primary target, the soft launch logically follows the primary audience: **soft launch in SA + AUS first**, expand later. Both stores allow per-country availability restriction at upload. iOS publishing is unblocked at the account level — Nico already has an Apple Developer Program membership. Google Play Developer ($25 one-time) is unconfirmed; revisit when Android shipping becomes relevant. |
| 2026-06-02 | Player Creation build plan written + reviewed; 2 sim questions logged | Ran `writing-plans` on the Player Creation spec → 12-phase Godot build plan at `docs/superpowers/plans/2026-06-01-player-creation-plan.md` (bootstrap → data/domain TDD → Creation scenes → permadeath hooks → **ADR 0012** authored in-plan). Two external review passes; key fixes folded in: archive **deep-duplicates the Player** before `clear_player()` (else an `ext_resource` orphan crashes the Hall-of-Fame path), loads use `CACHE_MODE_IGNORE`, autoloads register at the phase their script lands, Back preserves picks, Build recap added, Godot **4.3+ hard-required**. Plan is ready but **execution choice (subagent vs inline) not yet made**. Surfaced two open auto-sim questions (Player participation model · bowler-rotation policy) — logged under "Open design questions", Theme 7, non-blocking. |
| 2026-06-02 | claude.ai Player Creation + Hall of Fame design **incorporated**; V1 attribute model kept | 5 artefacts landed from the parallel claude.ai track and reconciled against CONTEXT.md + ADRs + the Player Creation spec/plan: **Hall of Fame spec** (`docs/superpowers/specs/2026-06-02-hall-of-fame-design.md`), **decisions log** (`docs/PLAYER-CREATION-DECISIONS.md`), **Godot build-notes** (`docs/PLAYER-CREATION-GODOT-BUILD-NOTES.md`), and two mockups (`docs/mockups/player-creation-v2.html` = **locked look**, `player-creation-v1.html` = variants archive). **Decision: keep the spec §3.5 V1 attribute model** (20 pts · 1–8 · 4 labels incl. WK-Batter) — the mockup's richer **32-pt / 1–10 / 9-label** classifier is logged as a **post-launch candidate**, so the reviewed build plan executes **unchanged** (Phases 1/2/6 untouched). HoF spec is **additive** to the plan: Phases 4/7 robust (`LegendEntry` stores a full `Player` snapshot → both arc-roles derivable); the real HoF screen later replaces the Phase-7 stub and gains additive `Legend` fields (`levelsWon`, `lifetimeTons`, `immortalised`, `retiredSeason`). AUS palette `#A86E00` gold-forward confirmed canonical (already in DESIGN_HANDOFF §16.2 + all mockups; the `#FFCD00` in the outgoing brief was stale). Reconciliation banners added to the 3 incorporated docs. |
| 2026-06-03 | Player Creation build started — Phases 0–2 done; 3 latent plan bugs found + patched | Executing the reviewed plan on branch `player-creation-build` (hybrid: **inline** Phases 0–2, **subagent-driven** from Phase 3, one phase per fresh chat). **Phase 0** Godot 4.6.3 project + GUT 9.6 vendored; **Phase 1** 8 domain types authored test-first; **Phase 2** classifier. **34 tests green**, 14 commits. Three latent bugs in the "reviewed" plan surfaced only at execution and were patched into it: (1) Phase 0 `--quit` boots the not-yet-existent main scene → use `--import`; GUT `v9.3.1` doesn't exist → `v9.6.0`; run `--import` after vendoring to register class_names; (2) `enum Label` collides with Godot's built-in `Label` node class → renamed `Kind` across Task 1.3 + Phase 2; (3) `*.gd.uid` files weren't being committed. Added a project **`CLAUDE.md`** capturing Godot dev conventions. |
| 2026-06-04 | Phase 5 done — first UI phase (Identity screen); eyeball step caught a test-invisible bug | **Phase 5** built the first playable screen: `AppearancePicker` subscene (4 placeholder tiles, `appearance_selected` signal, silent-select for Back-nav) + `Identity` scene (Screen 1 — country toggle, city dropdown that resets on country change, appearance gating until Country picked, portrait preview, seeded name + 🔄 re-roll, Next gated on `identity_complete()`, full draft hydration on Back). Subagent-driven (implementer → spec → code-quality review per task). **59 tests green, zero orphans**, 6 commits. **Key lesson:** all unit tests passed but the appearance tiles were *invisible* on screen — a `flat` Button tinted with `modulate` draws nothing. Caught only by running the real window (the plan's manual eyeball step), fixed by rendering tiles via `StyleBoxFlat` (with a gold selection ring). Unit tests assert "button exists / disabled toggles" and are blind to visibility — **UI phases are never done on green tests alone.** Gotcha added to project CLAUDE.md + the learn-by-seeing memory. |
| 2026-06-04 | Phase 6 done — Screen 2 (Build) | Built the attribute-allocation screen: `build.tscn` + `build.gd` — 4 sliders (each 1–8) bound to a transient `PlayerCreationDraft`, a live POINTS-REMAINING counter (white at 0, red otherwise), the live classifier label fed by Phase 2's `Classifier`, an identity recap (portrait tint + name·city·country), and a Confirm gated on `is_valid_creation_distribution()` that builds a `Player` via `from_draft` → `SaveManager.save_player` → emits `confirmed`; Back emits `back_pressed(draft)` for Identity rehydration. Slider→draft sync uses `set_value_no_signal` on push to dodge a re-entrancy bug (writing a slider value mid-update would read the *other*, not-yet-pushed sliders back into the draft and corrupt it). Subagent-driven (implementer → spec ✅ → code-quality ✅; one minor test-isolation leak — autoload `player_save_path` not restored — found in review and fixed, same discipline as Phase 4). **64 tests green** (59 + 5 new), zero orphans, 3 commits. Eyeballed in a real 390×844 window: renders + responds correctly. Nico flagged it "looks like a skeleton, not the final product" — **expected**: this plan ships flow + logic only; visual polish (locked look in `docs/mockups/player-creation-v2.html`, real portraits, palettes, backgrounds) is a separate Theme 6 pass applied after the flow is built end-to-end. |
| 2026-06-04 | Phase 7 done — main routing + stubs + lifecycle owner; flow walks end-to-end | **Phase 7** wires the whole Player Creation flow together. (7.1) `LifecycleManager` autoload — single owner of the "end Career" transition (`manual_retire`/`win_out` → `end_career` → archive + clear Player + emit `career_ended`); (7.2) 3 stub scenes (Starting Team picker, Season hub w/ Manual-retire confirm dialog + `[DEV]` win cheat, Hall of Fame listing LegendsArchive rows); (7.3) `main.tscn`/`main.gd` router keyed on `SaveManager.has_player()`. **66 tests green, zero orphans**, 4 commits, subagent-driven (spec + code-quality review per task; one isolation-leak caught in review — router test now restores `SaveManager` paths, same lesson as Phase 6). Creating `main.tscn` **cleared the benign load warning** present since Phase 1. The plan's assumed Identity/Build APIs matched the real Phase-5/6 scenes **exactly** (zero reconciliation). Eyeballed in a real window: full cold-start flow renders + routes correctly, retire + dev-win both archive to the Hall of Fame, and **the LegendsArchive persisted across a window restart** (proving Phase-4 persistence + Phase-7 lifecycle together, live). New learn-by-seeing lesson captured: during an eyeball walk, give Nico an explicit "look at / ignore" split so stub cosmetics (e.g. a too-wide retire dialog clipping its text) don't read as defects. **ADR 0012 deferred to Phase 10 per the plan** (the roadmap had mislabelled it as Phase-7 work). |
| 2026-06-04 | Phases 3–4 done — first subagent-driven phases; 2 more latent plan bugs found + patched | **Phase 3** name generator (`NameBanks` 8-bank stubs indexed by Country×Appearance, 10×10 each; seeded `NameGenerator.generate()` → `NamePair`). **Phase 4** persistence: `LegendEntry` + `LegendsArchive` resources + `SaveManager` autoload (registered in `project.godot`) persisting Player + Legends to `user://` `.tres`. Critical correctness point held + regression-tested: `archive_to_legends` **deep-duplicates** (`p.duplicate(true)`) so a loaded (path-bearing) Player is embedded **inline** (`sub_resource`), not as an `ext_resource` pointer to a file `clear_player()` immediately deletes — the spec reviewer empirically proved the guard catches the shallow-copy failure. Loads use `CACHE_MODE_IGNORE` (fresh disk read, no aliasing). **46 tests green, zero orphans**, 6 commits. Each task ran implementer → spec-compliance review → code-quality review (both gates passed). Two more latent plan-test bugs found + patched back into the plan: (1) `:=` type-inference through the untyped `var sm` receiver is a **parse error** → use `=`; (2) the test created an orphaned `Node` per run → `sm.free()` in `after_each`. |
| 2026-06-06 | Theme 7 started — `resolve_ball()` sim atom built + merged | First slice of the match engine (ADR 0004). Decomposed Theme 7 into 7a sim core → 7b Season wrapper → 7c balance harness, and the sim core itself bottom-up (ball → innings → match → Intent). Built the smallest rung: pure deterministic `BallResolver.resolve_ball()` — two-stage logistic (wicket: Composure vs Attack; runs: Power vs Control), Intent couples both, coefficients in a `BallTuning` resource (strawman, harness-swept later). Full brainstorm→spec→plan→inline-build→PR cycle; 16 new tests (97 green). Design sandbox `docs/mockups/ball-resolution-v1.html`. PR #3 merged to `main`. Neither open auto-sim question blocked the atom — they're innings/match-level. |
| 2026-06-07 | Theme 7a rung 4b — per-phase bowling rotation built + merged | Second half of rung 4 (PR #8): the bowling side gets its first decision lever, mirroring 4a Intent and realizing `setNextBowler` (ADR 0006). `BowlingPlan` (`enum Kind {PACE, SPIN}`, one type per phase, `for_over()`, factories; bare `new()` == textbook) + `BowlingAttack` (derives pace/spin `(attack,control)` from a base pair ± tilt const, floored at 1; `profile(kind)→Vector2i`). Threaded as optional trailing params through `simulate_innings()` (per-over profile) and `simulate_match()` (opt-in `player_bowling_plan`: null → no rotation, byte-identical to 4a; set → Player team bowling uses the plan routed to the opposition's batting innings, opposition bowls a fixed `textbook()` AI default). **Real EV trade (ADR 0003):** pace = more wickets + more runs, spin = containment — neither dominates. **Scope calls:** narrowed 4b to the rotation *mechanism* — **bowler husbanding (pool + 4-over cap + decay) deferred to optional 4c**; **Player stays bat-only**. Built inline test-first (4 tasks). 10 new tests (154, was 144): tilt+floor, phase mapping+factories, innings null==scalar + determinism + directional run-rate, match null==4a + routing + determinism. Spec `docs/superpowers/specs/2026-06-07-bowling-change-design.md`, plan `docs/superpowers/plans/2026-06-07-bowling-change.md`. **Match sim core (7a) now functionally complete; next = Season wrapper (7b).** |
| 2026-06-07 | Theme 7a rung 4a — per-phase batting Intent built + merged | Split rung 4 into **4a (Intent)** + **4b (Bowling Change)** — they're cleanly separable and very different in size; 4a is small, 4b is a chunky new subsystem. Built 4a (PR #7): the sim stops hardcoding `Intent.BALANCED` — a small `IntentPlan` value object (3 phase bands: Powerplay 1–6 / Middle 7–15 / Death 16–20; `for_over()` lookup; `balanced()` + `textbook()` factories; phase boundaries as constants `POWERPLAY_OVERS=6`/`DEATH_START_OVER=16`) is threaded as an optional trailing param through `simulate_innings()` and `simulate_match()`. `null` → all-BALANCED → byte-identical to rung 3 (all prior tests stay green). Routed to the **Player's batting innings only**; opposition stays Balanced (opponent batting intelligence = later rung). This is the sim's **first real decision input** — the lever the ADR 0003 naive-vs-optimal skill-gap metric measures; the Theme-6 swipe-card later writes into this same seam. Realizes the `setIntent` verb of ADR 0006 (data path only). Built **inline test-first** (not subagent-driven — 3 tiny pure-domain tasks, zero test-isolation surface). 11 new tests (144, was 133): phase mapping, factories, null==balanced regression, determinism-with-plan, directional coupling (aggressive both outscores **and** out-dies defensive), match routing both directions (via the first-innings-uses-initial-RNG isolation trick), match determinism. Full brainstorm (Nico chose "brainstorm then check in", then "resume AFK") → spec → plan → build → PR cycle. Spec `docs/superpowers/specs/2026-06-07-intent-plan-design.md`, plan `docs/superpowers/plans/2026-06-07-intent-plan.md`. |
| 2026-06-07 | Theme 7a rung 3 — full-match sim built + merged | Third sim rung (PR #6): `MatchResolver.simulate_match()` calls `simulate_innings()` twice + chase + cricket-standard result. Chose the **thinnest wrapper** (simplest-first): constant bowling, raw-int inputs, caller-supplied `player_bats_first` bool (toss deferred to 7b). **Settled the bowler-rotation open question as deferred** — constant bowling both sides; real per-over rotation lands at rung 4 (Bowling Change KM + opponent AI policy). Two backward-compatible extensions to `simulate_innings()`: optional statted Player (`null` → all-derived opposition innings) + optional chase `target` (stop the instant `total >= target`). `_decide_result()` extracted pure so the tie branch is testable on constructed innings. `MatchResult` holder (`Outcome` enum, margins, `margin_text()`). Full brainstorm→spec→plan→**subagent-driven** build (4 tasks, per-task spec + quality review + holistic final review)→PR cycle; 18 new tests (133 green, was 115). Determinism via shared rng consumed in fixed innings order. Spec `docs/superpowers/specs/2026-06-07-full-match-design.md`, plan `docs/superpowers/plans/2026-06-07-full-match-plan.md`. |
| 2026-06-07 | Theme 7a rung 2 — single-innings batting sim built + merged | Second sim rung: `InningsResolver.simulate_innings()` loops the ball atom into one deterministic T20 innings. **Settled the participation open question: build-driven batting position** — a batter-shaped build opens, a bowler-shaped build bats the tail; how many balls the Player faces emerges from order + strike rotation (chosen over fixed-quota; full team innings chosen over Player-only). 11 batters (Player statted + 10 derived from team strength), weakening tail, real strike rotation, 10-wkt/120-ball termination (fixes ball-spec §9 "no clock" gap). New `InningsTuning` strawman resource. Format confirmed T20/20-over from CONTEXT.md (not re-litigated). `InningsResult` holds aggregates only — no per-ball log (perf). Full brainstorm (w/ interactive innings sandbox `docs/mockups/innings-sim-v1.html`)→spec→plan→**subagent-driven** build→PR cycle; 18 new tests (115 green); even-contest avg ~123, inside the sane 110–175 T20 band. Plan had a self-caught off-by-one in two task test-counts. PR #5 merged to `main`. Also: project CLAUDE.md gained a "be concise" communication-style rule. |
| 2026-06-05 | Phases 8–10 done — lifecycle tests + ADR 0012; Player Creation build functionally complete | **Phase 8** (Manual retire) + **Phase 9** (Win-out) added `tests/unit/test_lifecycle_manager.gd` (3 tests) covering both end-state transitions already wired + live-verified in Phase 7: `manual_retire`/`win_out` → archive with the right `end_reason` (`retired`/`won`) + `career_ended` signal, plus the no-player no-op-warning path. These **retro-test existing Phase-7 code**, so they went green on first run (verification, not red→green TDD); the `LifecycleManager`/`LegendEntry`/`SaveManager` APIs matched the plan's assumed test verbatim (zero reconciliation). **Phase 10** wrote **ADR 0012** (Player lifecycle — hard permadeath: two triggers — Win-out + Manual retire — through a single `end_career(reason)`; auto-drop/age-based/relegation all deferred to Theme 9, form-anchored not Team-anchored when they land) and updated this roadmap. **Ran inline, not subagent-driven** — Nico flagged the per-phase stop/start as too granular, and these are tiny test-only/doc phases against code that already existed and passed live, so the subagent ceremony added little. **69 tests green, zero orphans**, 4 commits (`fa312a4`, `44bcb30`, `b9540bb` + roadmap). Only Phase 11.2 (final manual cold-start walk) remains; the automated sweep (Phase 11.1) is green. |

---

## Reference docs

- `CONTEXT.md` — domain glossary · source of truth for terminology
- `docs/adr/` — design decision records (0001 target market · 0002 V1 game shape · 0003 skill model · 0004 auto-sim architecture · 0005 Manager Boost no-menu · 0006 typed-effect palette · 0007 Joker authoring grammar · 0008 currency name + mark · 0009 Team durability · 0010 around-the-match navigation shell · 0011 global rank via Tour-distribution percentile · 0012 Player lifecycle hard-permadeath)
- `docs/joker-pool-v1.md` — all 45 Jokers authored (theme 4 content)
- `docs/superpowers/specs/` — implementation specs (`2026-06-01-player-creation-design.md` — Player Creation flow + permadeath lifecycle · `2026-06-02-hall-of-fame-design.md` — Hall of Fame / Legends-wall screen, replaces the build plan's HoF stub later · `2026-06-06-ball-resolution-design.md` · `2026-06-07-innings-sim-design.md` · `2026-06-07-full-match-design.md` · `2026-06-07-intent-plan-design.md` · `2026-06-07-bowling-change-design.md` · `2026-06-07-season-wrapper-7b1-design.md` · `2026-06-07-season-league-7b2a-design.md` · `2026-06-08-season-playoffs-7b2b-design.md` · `2026-06-08-balance-harness-platform-7c1-design.md` · `2026-06-08-player-as-bowler-design.md` — the match-sim rungs incl. 4a Intent, 4b bowling, 7b-1 Team/stars/toss, 7b-2a league, 7b-2b playoffs+outcome, 7c-1 balance-harness platform, Player-as-bowler)
- `docs/superpowers/plans/2026-06-01-player-creation-plan.md` — reviewed 12-phase Godot build plan for Player Creation (**the build authority; next to execute**)
- `docs/PLAYER-CREATION-DECISIONS.md` — claude.ai hi-fi decisions log (Player Creation + Hall of Fame); see its reconciliation banner — V1 builds the spec §3.5 4-label model, not the doc's 9-label matrix
- `docs/PLAYER-CREATION-GODOT-BUILD-NOTES.md` — claude.ai engine-binding notes (data binds / transitions / palette tokens); design reference, *not* the build authority (the plan is)
- `docs/PLAYER-CREATION-HANDOFF.md` — outgoing brief sent to claude.ai (archival; its AUS `#FFCD00` value is stale — DESIGN_HANDOFF §16.2's `#A86E00` is canonical)
- `docs/brand/` — Tons (₸) currency mark · SVG assets and usage notes
- `docs/DESIGN_HANDOFF.md` — locked design system (chrome, portraits, KMs, IP rules, palette, fonts, motion, country system, hi-fi addendum §16, around-the-match design system §17)
- `docs/THEME-5-HANDOFF.md` — claude.ai mockup brief for theme 5 (archival; theme 5 now closed)
- `docs/design-handoff-from-claude-2026-05-31.md` — return handoff from claude.ai for theme 5 (Australia palette fix + Collection screen scope-add)
- `docs/RESEARCH_LAUNCH_PLAYBOOK.md` — full launch research (not on build critical path)
- `docs/mockups/ball-resolution-v1.html` — interactive single-ball sandbox (Theme 7a rung 1)
- `docs/mockups/innings-sim-v1.html` — interactive single-innings sandbox (Theme 7a rung 2: build-driven position, strike rotation, live scorecard + run-worm, avg-of-200 demonstration)
- `docs/mockups/star-winrate-v1.html` — **real-Godot-sim** win-rate-vs-star-gap chart (Theme 7b-1; data from `tools/season_preview.gd`; the rising S-curve proving stars→outcome; per-match dots + standard-deviation panel; first seed of the 7c balance harness)
- `docs/mockups/league-finish-v1.html` — **real-Godot-sim** playoff-qualification + average-finish vs Player ★ chart (Theme 7b-2a; data from `tools/league_preview.gd`; full league phase over random fields)
- `docs/mockups/season-outcome-v1.html` — **real-Godot-sim** beat-rate + win-Final-rate vs Player ★ chart (Theme 7b-2b; data from `tools/season_outcome_preview.gd`; full Season = league + playoffs over random fields)
- `docs/mockups/distribution-viewer-v1.html` — **reusable** before/after distribution-overlay viewer (Theme 7c-1; `scripts/harness/` `Sweep`+`Distribution`; demoed on Player-build/skills via `tools/sweep_skills.gd`; swap its `DATA` for any future sweep — jokers, economy, …)
- `docs/mockups/match-screen-final.html` — locked in-Match design (4 frames)
- `docs/mockups/in-match-hi-fi-v1.html` — claude.ai hi-fi pass for in-Match
- `docs/mockups/around-the-match-v1.html` — claude.ai hi-fi pass for around-the-match screens (16 frames across 7 sections)
- `docs/mockups/player-creation-v2.html` — claude.ai hi-fi pass for Player Creation + Hall of Fame (**locked look**: portrait-anchored Identity · drag sliders · portrait-card Legend rows · gold retire badge)
- `docs/mockups/player-creation-v1.html` — Player Creation variants exploration (1A/1B/1C · 2A/2B · 3A/3B/3C; superseded by v2 for the locked directions)
- `docs/mockups/playable-prototype.html` — interactive 70-sec demo
- `docs/mockups/player-avatars-styles.html` — 3 art-style comparison
- `docs/mockups/the-t-mark-v1.html` — Tons (₸) currency-mark exploration

---

## Deferred (launch track — not blocking the build)

Marketing · TikTok/Insta devlog · soft launch playbook · Apple/Google submission · ad mediation · multi-language beyond English/Hindi essentials · battle pass / live ops · multiplayer

When ready, read `docs/RESEARCH_LAUNCH_PLAYBOOK.md`.

---

## Done so far

- Brainstorming session: in-match design (8 key-moment types defined; 3 fully designed)
- Player avatar system: Card Portrait + 3 layers (achievement / wear / identity) + split-bg evolution
- Hi-fi pass on claude.ai: stadium-scene CRR worm, jokers panel, water-meter boost, 10-nation palette
- Design handoff doc (full system spec)
- Engine decision (Godot)
- Launch playbook research saved (`docs/RESEARCH_LAUNCH_PLAYBOOK.md`)
- 7-theme roadmap (this doc)
- `grill-with-docs` skill installed for next session
- Scope + Game shape theme — V1 locked as a roguelite cricket-career
- `CONTEXT.md` domain glossary + ADRs 0001 (target market) & 0002 (V1 shape)
- Git repo initialised + pushed to GitHub (private: `github.com/nico101rsa/cricket-sim`)
- Gameplay & balance theme — auto-sim architecture (ADR 0004), skill model (ADR 0003), all 8 Key Moments designed, Manager Boost simplified (ADR 0005), 6-verb effect palette (ADR 0006)
- Portrait redesign — full-bg skill + facial-expression Form supersedes the split-bg model
- Currency naming + mark — **Tons (₸)**, two-bail T glyph (ADR 0008, `docs/brand/`)
- Jokers & meta loop theme **closed in full** — architecture (Shop cadence, authoring grammar ADR 0007, ~45-Joker pool across 6 archetypes, Carry-Over, slot 4 unlock, Offer cadence, Team durability ADR 0009) plus content (all 45 Jokers authored at `docs/joker-pool-v1.md` — 24 Common / 15 Rare / 6 Legendary, magnitudes V1 strawman pending balance harness).
- Around-the-match screens theme **closed in full** — Season hub, Pre-match, Shop ("Kit Room" scene with Kit Manager persona), Offer (with carry-over picker as footer), Result, Career Grid (diagonal-ladder topology), Career Records (global rank `#X / 2,112` computed analytically — ADR 0011) all designed in hi-fi (`docs/mockups/around-the-match-v1.html`). Navigation shell — Season hub as home + forced Shop/Offer interludes + no Main Menu — locked in ADR 0010. Australia palette fix (gold primary + green accent). **Onboarding closed** — Player Creation spec at `docs/superpowers/specs/2026-06-01-player-creation-design.md`: 2-screen Identity → Build flow, hard-permadeath lifecycle (Win + Manual retire), 4 Appearance buckets per Country, cricket-pun name gen, 20-point attribute slider, live classifier label. Settings still deferred.
- **Player Creation build — Phases 0–7 done** (branch `player-creation-build`, 2026-06-03/04): Godot 4.6.3 project + GUT 9.6 test harness, 8 tested domain types, classifier (Phases 0–2); name generator — `NameBanks` + seeded `NameGenerator` (Phase 3); persistence — `LegendEntry`/`LegendsArchive` + `SaveManager` autoload with inline deep-duplicate archive + fresh-read loads (Phase 4); **first UI — `AppearancePicker` subscene + `Identity` screen (country/city/appearance/name + re-roll, Back-nav hydration), Phase 5**; **Screen 2 `Build` — 4 attribute sliders + live points counter + live classifier label + identity recap + Confirm that finalises & persists the `Player`, Phase 6**; **flow wiring — `LifecycleManager` autoload (single "end Career" owner) + 3 stub scenes + `main.tscn` router (cold-start → Creation → stubs; existing save → Season hub; `career_ended` → Hall of Fame), Phase 7**. **66 tests green, zero orphans**, 33 commits, test-first throughout. Phases 3–7 ran subagent-driven (implementer → spec review → code-quality review). Five latent plan bugs found + patched in earlier phases; Phase 5 added the invisible-UI lesson (flat Button + `modulate` = invisible; use `StyleBoxFlat`); Phase 7's eyeball walked the **whole flow end-to-end in a real window** (renders + routes correctly, retire/dev-win archive to Hall of Fame, LegendsArchive persisted across a window restart) and cleared the benign `main.tscn` load warning. The full Player Creation flow is now runnable end-to-end (skeleton/unstyled by design — visual polish is Theme 6); next is Phase 8 (manual-retire automated tests). Project `CLAUDE.md` holds the Godot dev conventions.

---

## Notes

- **7c balance watch-list — mid-tier flatness (logged 2026-06-08):** at the tuned `spread` 1.5, the 2.0–3.0★ band is bunched (all ~42% top-4 league qualification; ~2% each to top the table), then there's a "tier wall" jump to 3.5★+ (~82% top-4). A hard top-4 cutoff is naturally an S-curve (sigmoid), so the mid tiers sit on its flat lower shoulder and feel undifferentiated. Revisit in the **7c balance harness** if the mid-tier should feel more distinct (levers: a non-linear `percentile` mapping, a slightly higher `spread`, or per-Tour band shaping). Upset stats at spread 1.5: a ≤2★ team tops a league ~2.8% of seasons; a 0.5★ team beats a 5.0★ team ~0.23% of single matches.
- **Seeing games:** the sim is headless with no per-ball log, so games can be shown as a **scorecard** (a throwaway runner through the real `simulate_match()` does this on demand — ask "show me a game"). To *watch a game unfold ball-by-ball* would need an optional **delivery-log** addition to the sim — a small future rung, natural fit alongside the Theme 6 in-match screen.
- "Successful" = I play it and enjoy it. Anything beyond is bonus.
- Playtest threads through every theme — budget time for it; fun doesn't emerge from spec docs.
- If a feature would take >1 weekend to design AND build, decide V1 vs V2 first.
- **Balance harness** is the tuning workhorse — pure Python or Godot-headless module that runs thousands of Seasons over a parameter grid, measures the optimal-vs-naive skill gap (ADR 0003), and flags overpowered Jokers / underpowered Tour cells. Lives in the Tech foundation theme but every numerical decision from theme 3 onward routes through it. The auto-sim's headless / deterministic-seedable design (ADR 0004) exists precisely to enable this.
